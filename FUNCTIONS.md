# CovariateSearcher — Function Reference

**What this is:** an agent-facing map of *every* function in `R/`, so future sessions don't have to re-read the whole codebase. Originally generated from a full read of the pre-refactor code (v0.1.30). **Last audited 2026-08-20 against v0.4.7: 29 files in `R/`, 145 top-level functions, 90 exports in NAMESPACE** — every defined function appears below. Keep it in sync as the modular reorg lands.

**Legend:** `Exp` ✓ = exported (`@export`), · = internal/documented-internal. *Side effects* flag file writes, `bbr::` calls, `<<-`, `stop()`, hardcoded paths. *Nested* = a closure defined inside another function (not top-level).

**Housekeeping:** this file is a dev/agent aid, not package content — `^FUNCTIONS\.md$` is in `.Rbuildignore` (done), so it doesn't ship or trip `R CMD check`.

---

## Target-module map (current file → planned home)

| Current file(s) | Planned module |
|---|---|
| `CovariateSearchr.R`, `imports.R`, `utilities.R` | `utils.R` (+ package scaffolding stays) |
| `file-io.R`, `monitoring-files.R`, `plot_nonmem_iterations.R` (ext reader), `model-output-tables.R` (matrix/`$THETA` parsers) | `nonmem-io.R` |
| `initialization.R`, `database-management.R`, `model-discovery.R`, `validation.R` | `search-state.R` |
| `model-modification.R` | `model-modification.R` |
| `full_scm_search.R`, `scm-algorithm.R`, `scm-execution.R`, `scm-evaluation.R` | `scm-engine.R` |
| `scm-selective-forward.R`, `scm-backward.R` | `scm-method-*.R` (+ engine bits) |
| `recovery-detection.R`, `recovery-actions.R` | `recovery.R` |
| `reporting.R`, `scm-results.R`, `model-output-tables.R` (tables/plots) | `reporting.R` |
| `covariate-formula.R` (**exists**) | `covariate-formula.R` |
| `sample-theta-uncertainty.R`, `covariate-table.R`, `simulate-scenario-profiles.R`, `plot-exposure-forest.R` | `forest-plots.R` (**done**); `apply-covariate-model.R` + `build-scenario-parameters.R` + `covariate-boxplots.R` kept separate |
| `continue-search.R`, `resume-reconstruct.R` (**newer than this map**) | resume/checkpoint path — not in the original reorg plan |

---

## Cross-cutting hazards & consolidation candidates (read before any merge)

**Duplicate implementations (merge targets — but preserve contracts):**
- **`.ext` readers ×3:** `read_nonmem_ext` (file-io.R → `found/ofv/parameters`, FINAL `-1e9` line, flat+subdir), `read_ext_file` (monitoring-files.R → `status/current_ofv/iterations/has_estimation_issues/…`, last/current line, subdir-only), `read_ext_iterations` (plot_nonmem_iterations.R → tidy data.frame, consumed positionally by the sim sampler). **Different OFV semantics + file locations — do not conflate.**
- **`.cov`/`.cor` matrix parsers ×2:** `calculate_condition_number` (model-output-tables.R, reconstructs lower-triangular) vs `.read_nonmem_matrix` (forest-plots.R, assumes labelled square).
- **`$THETA` name/trans parsers ×3:** `extract_params`/`extract_model_params` (model-output-tables.R) vs inline in `apply_covariate_model` vs inline in `create_covariate_table`.
- **Categorical-level decode ×2+:** `decode_cov_level` (model-output-tables.R, the only unit-tested fn) vs nested `decode_level` (forest-plots.R) vs the inline decode in `model_add_cov`.
- **Covariate formula algebra ×2 (highest risk):** `model_add_cov` *writes* NONMEM factors; `apply_covariate_model` *reconstructs the same math in R*. Must stay in sync. Also, `model_add_cov`'s formula block is **duplicated verbatim within itself** (~L404-408 and ~L490-494).
- **name→tag idiom** `names(tags)[tags==name]` repeated ~6× in scm-selective-forward.R + full_scm_search.R; overlaps `get_covariate_tag_from_name`.
- **"Get covariates" family (names lie about source):** `get_model_covariates_from_db` (reads **bbr files**, returns tag **values**), `get_model_covariates_from_files` (reads bbr files, returns tag **names**), `get_covariates_from_models` (DB, many models). **Do not collapse blindly.**
- **Run-success test ×2 (added 2026-07-30 — must stay identical):** `get_model_status_from_files` (file-io.R, returns a status string; used by base-model validation + discovery) and `update_model_status_from_files`'s nested `read_all_model_files` (validation.R, writes the DB row) now apply *the same* rule in the same order: run finished (`.lst` present, not `incomplete`) → usable OFV (non-NA, finite, `abs <= 1e10`, no parameter at `abs >= 8.9999e5`) → `.cov` present when `require_cov_step`. Both resolve output location via `read_nonmem_lst()$file` and look for the `.cov` in `dirname()` of it. Neither has a route to `"completed"` that skips the OFV test: a `.lst` reporting completion with a missing or unparseable `.ext` is `failed` in both. **If they drift, a model's verdict depends on which reader looked at it.** `completed_with_issues` was retired from this path and is no longer produced anywhere in the package. The flag itself is threaded `initialize_covariate_search(require_cov_step=)` → `validate_base_model_for_search` + `initialize_search_config` (stored in `search_config`, **before** `discover_existing_models` runs) → read back via `%||% TRUE` by discovery, `update_model_status_from_files` and `run_automated_scm_testing` (which only overwrites it when the caller supplied it). **Extended 2026-07-31:** the agreement now also covers an *unreadable* listing — both readers call `read_error` `failed` (previously `update_model_status_from_files` reported it as `in_progress`, which the monitoring loop waits on forever), and both read listings only through `.read_listing_lines()` (latin1 + `skipNul`), never bare `readLines()`. The two readers still **name the unfinished state differently** — `update_model_status_from_files` writes `"in_progress"`, `get_model_status_from_files`/discovery write `"incomplete"` — and both spellings sit in checkpoints already on disk, so neither can be renamed; any "is this still running?" guard must go through `.is_pending_status()` rather than an inline status vector.

**Acceptance rule ×1 (consolidated 2026-08-07 — no longer a merge target):** "does this model meet the criteria" (status `completed` + per-covariate-df ΔOFV threshold + RSE limit) used to be re-derived independently at every decision site. It now has **one** implementation, `.evaluate_model_criteria()` (scm-evaluation.R), read by `select_best_model`, `.best_acceptable_model`, `get_significant_models_from_step`/`get_step_models`, `evaluate_removal_impacts`, and `create_scm_results_table`'s nested `get_selection_info`; the RSE limit itself resolves only through `.resolve_rse_threshold()` (utilities.R). **The `phase` argument is not cosmetic** — forward and backward store `delta_ofv` with opposite signs *and* compare it in opposite directions, so a backward step judged with the forward default silently reports the wrong models. **And a phase-aware evaluator is not by itself enough: the candidate *pool* handed to it must be phase-consistent too.** A forward pick over a set that includes backward rows misreads every one of them — a removal that badly worsened the fit looks like a large improvement — no matter which `phase` was passed. This bites any search that *begins* with backward elimination, which then has such rows in the database from the start. `.scm_backward_rows(db)` (continue-search.R) is the row-level filter for this, and `run_scm_selective_forward` applies it at **all four** of its candidate pools; the test *"every forward candidate pool in the file carries the phase filter"* greps the source and fails if any pool lacks it, so a newly added fifth pool cannot silently reintroduce the misread. The filter tests `phase` **OR** `action` (via `.is_removal_model`), not `phase` alone: a retry of a backward removal is written `phase="retry"` yet later receives a backward-signed `delta_ofv`, so phase-only let it into every forward pool. Matching on `action` closes that because it is the same predicate validation.R:336 uses to choose the sign. The base row is matched by neither test (`phase="base"`, `action="base_model"`) and so stays in scope on purpose — the redemption-base fallback needs to find `base_model_id` in that same table. See the two-field note under R/continue-search.R for the sign-writer audit. **One remaining exception, and it is deliberate:** `evaluate_removal_impacts` calls the evaluator but **discards its `meets_threshold`**, recomputing `meets_ofv & meets_rse` without the `status == "completed"` term — by design, because eligibility there is the caller's `completed_models` argument, and `submit_and_wait_for_step()` hands back that list wholesale under `auto_submit = FALSE`, so re-gating on DB status would stall the manual workflow. Do not "fix" it back to `meets_threshold`. (`generate_scm_report` was the other exception — no RSE term, `df=1` hardcoded, final model by `which.max(step_number)`. **Rewritten 2026-08-07** to read every verdict from the evaluator and to track the final model through the accepted winners, so the report and the search can no longer disagree.) The companion invariant is the **sign**: `.signed_delta_ofv()` (utilities.R) is the one place the ΔOFV convention is chosen, off the same `.is_removal_model()` predicate, so a stored delta and the rule that reads it stay in agreement — see its `direction` note under R/utilities.R for when a caller may override it.

**Schema declared in 5 places (must all stay in sync / all get `stage`):** canonical `initialize_search_database_core` (19 cols as of 2026-07-30, `created_by_search` added) · self-heal `required_cols` in `validation.R` (13-col subset) · self-heal in `scm-selective-forward.R` · `model-discovery.R` rebuilds the 19-col row by hand · row-builders `add_covariate_to_model`/`remove_covariate_from_model` (`bind_rows`) + `create_retry_model` (**order-sensitive `rbind` on a blanked `db[1,]` template** — hence its `.ensure_provenance_column()` call). A checkpoint predating a new column is only migrated for `created_by_search`, via `.ensure_provenance_column()` in `load_search_state`/`create_retry_model`; the other four sites have no migration path.

**`action` / `phase` vocabulary (display registry must cover all):**
- `action` written: `base_model`, `add_covariate`, `remove_covariate`, `add_single_covariate`, `remove_single_covariate`, `retry`, `manual_modification`.
- `phase` written: `base`, `forward_selection`, `covariate_removal`, `backward_elimination`, `retry`, `manual`, `individual_testing`.
- Display generators (`generate_step_display`/`_changes_display`/`_step_description`) only recognize `{base_model, add_covariate, remove_covariate, retry}` → **everything else renders "Unknown: X"**.

**Confirmed bugs (fix during the relevant phase):**
- ~~`run_univariate_step` error handler `<-`~~ **FIXED 2026-07-18**: error handler used `<-` (handler-local) → thrown-error failures dropped from tracking → now `<<-` (scm-execution.R:153-154), matching the file's existing `<<-` callbacks.
- ~~`remove_covariate_from_model` action label~~ **FIXED 2026-07-18** (A1): wrote `action="remove_single_covariate"` → "Unknown" in tables (backward path was only patched by `scm-backward.R:155`); now writes `"remove_covariate"` at root so manual removal displays correctly.
- ~~Retry misclassification~~ **FIXED 2026-07-18**: `grepl("\\d{3}$")` flagged `run100`+ as retries → now `grepl("^run\\d+001$")` (was 6 sites; the 2 yaml-analysis helpers later removed in the dead-code sweep — remaining: `process_estimation_issues`, `update_model_counter`, `generate_recovery_report`, `discover_models`). Residual false positives only for normal models ending in `001` (run1001…).
- Excluded-covariate "final testing" block in `full_scm_search.R` is **dead** (filters `phase=="forward"` which is never written; also gated by `run_forward && !run_final_backward`, false for `full_scm`). → retire (owner dropped `excluded_only`).
- ~~`cat.power` read-side bug~~ **FIXED 2026-07-19 (registry, Step ③)**: `apply_covariate_model` previously gated the categorical `IF`-parse branch on `status=="cat"` alone, so `cat.power` (e.g. dose 35/70/125/150) wrongly entered it (no `IF(COV.EQ.x)` block → mis-reconstructed). Now BOTH `model_add_cov` (write) and `apply_covariate_model` (read) dispatch on the registry's `categorical` flag (`get_covariate_formula(status, formula)$categorical`): `cat.linear` → per-level `IF/ELSEIF`; everything else incl. `cat.power` → single factor from the same registry entry (`nonmem` write / `r_eval` read), so write & read can't drift.
- ~~`generate_phase`~~ **removed 2026-07-18** (dead, unexported).
- ~~Parenthesized RHS hides earlier covariates from removal~~ **FIXED 2026-08-11** (found the same day while indexing `.wrap_rhs_in_parens`; write side vs remove side). The wrap is now *conditional*: `model_add_cov` calls `.wrap_rhs_in_parens` only when `.rhs_needs_paren_wrap()` finds a `+`/`-` at paren depth 0, so a summed `TV_` line is wrapped exactly once and a flat multiplicative chain is left flat. Two adds on one parameter now give `TV_CL = THETA(1) * (1 + (AGE-40) * THETA(5)) * ((WT/70)**THETA(6))` instead of the nested `((THETA(1))*(…))*(…)`, and `strip_covariate_factors`' depth-0 split still sees every factor. **Re-verified by execution 2026-08-11** (both functions loaded from source and driven through the real `model_add_cov` append block): removing the earlier, the latest, or a middle covariate all rewrite the line and drop the `THETA(n)` reference, on a plain `TV_` line, a commented line, a genuinely summed base RHS, and the additive/log placement. The former residual — `remove_covariate_from_model` only *logging* `lines_modified` — is now closed too; see the next entry.
- ~~Silent `$THETA` deletion when no `$PK` line matched~~ **FIXED 2026-08-12**: `remove_covariate_from_model` used to log `lines_modified` and carry on, so any covariate shape that escaped `strip_covariate_factors`' depth-0 scan (or the categorical `* beta_…` gsub) still had its `$THETA` line deleted and the survivors renumbered — leaving a live `THETA(n)` in `$PK` bound to a *different* parameter, with no error and a model that still runs. There is now a `stop()` immediately after the Step-5 strip loop (model-modification.R:1283) whenever `lines_modified == 0` while `theta_numbers_to_remove` is non-empty, i.e. the name matched a `$THETA` line but nothing in the equations changed. **Verified by execution** (diff-auditor drove the real function, not a re-implementation). Three known consequences of the guard are recorded under "Known limitations of the `lines_modified` guard" below — none of them reopen the silent-corruption path.

**Known limitations of the `lines_modified` guard (audited 2026-08-12, all three accepted as non-blocking):**
- **Orphan-`$THETA` cleanup now errors instead of succeeding.** A model carrying a `beta_<COV>_<PARAM>` `$THETA` line whose `$PK` reference is already gone used to be tidied up by `remove_covariate_from_model` (delete the line, renumber, done). It now hits the guard, since no equation changes. That shape is not produced by the search itself; if it turns up, the `$THETA` line has to be removed by hand.
- **The `stop()` can fire after the child model file exists.** Step 2 runs `bbr::copy_model_from` *before* the control stream is edited in Steps 3-12, so when the guard trips the `run<N>` files are already on disk while the DB row (Step 11) is never appended and the incremented `model_counter` never reaches the caller (the error propagates; the modified `search_state` is discarded). A backward-elimination step that keeps going past the error is therefore working around an orphan `run<N>` on disk that no database row references — clean it up before resuming, rather than assuming the number is free.
- **`model_add_cov` called directly can still mis-write a conditional-TV model.** The `has_tv_line` check lives in `detect_param_transform`, and the `stop()` that turns a missing line-start `TV_<param>` into an error lives in `validate_param_transformations`. The public path (`initialize_covariate_search` → `validate_setup`) always runs the latter first, so a search cannot reach the bad write. A direct call to the exported `model_add_cov` skips it and can still append the covariate to the `PARAM = EXP(MU+ETA)` line. Validate first, or accept that responsibility.

**Rejected designs (considered, implemented, reverted — do not re-propose):**
- **Gating model success on NONMEM's termination verdict** (tried and reverted 2026-08-07). The idea: have `update_model_status_from_files` consume `read_nonmem_lst()$status == "failed"` and mark the model failed, since the listing already classifies `MINIMIZATION TERMINATED` / `FATAL ERROR` and the caller was discarding it. It looks like free rigour. It is not:
  - It contradicts a **deliberate** decision stated at file-io.R:466-469 — *"How minimisation ended is deliberately not part of it: a run that stopped on rounding errors but produced both is a success."* Success is a usable OFV plus a covariance matrix, nothing else.
  - `MINIMIZATION TERMINATED DUE TO ROUNDING ERRORS` matches `hard_failure_patterns` and is **the most common non-`SUCCESSFUL` outcome in covariate work**; such runs normally do produce a usable OFV *and* a `.cov`. Gating on it would have failed most usable covariate models, and the search would have reported "no significant covariates" — a wrong scientific answer, arrived at silently.
  - The gate returned **before the `.ext` was read**, so it also suppressed OFV/RSE recording for those rows: the OFV the run really did produce vanished from the database and from every report.
  - The obvious defence — make the hard-failure test order-aware, so a failure marker only counts when no success marker follows — has its own false negative. It asks "is there a success after the *first* failure", so `TERMINATED → SUCCESSFUL → TERMINATED` reports `completed`. A three-`$EST` sequence (SAEM → IMP → FOCE) is ordinary, so this is not a corner case either.
  - **The real convergence gate already exists and is `require_cov_step`:** a successful covariance step is stronger evidence of identifiability than a termination string, and it is already threaded through init → config → both readers → `.evaluate_model_criteria`'s RSE rule.
  Net: `read_nonmem_lst()$status` stays a *listing* verdict that no caller treats as disqualifying, and both run-success readers keep the OFV+`.cov` test above.

**Dependency/NAMESPACE hygiene — RESOLVED (verified 2026-07-28):** `mvtnorm`, `ggplot2`, `yspec` are in `Imports:`; `mrgsolve`, `devEMF` in `Suggests:`. The 5 sim `@export`s that used to be missing (`sample_individual_thetas`, `build_scenario_parameters`, `simulate_scenario_profiles`, `plot_exposure_forest`+`theme_forest`, `create_covariate_boxplots`) are all in NAMESPACE now. Still un-declared: base-priority `tools`/`grid`/`grDevices`.

**Removed on `feature/package-rebuild`:** `analyze_model_covariates_yaml`, `analyze_model_from_logic`, `get_model_parent_yaml`, `get_dropped_covariates`, `generate_phase`, `get_model_covariates`, `read_model_yaml` (dead-code sweep 2026-07-18, last two are cascade orphans); `resume_selective_forward` (2026-07-18); `data-operations.R` + its 3 exported functions.

**`<<-` scope hazards (break if extracted into functions):** `submit_and_wait_for_step` error callbacks (scm-execution.R L323-324, L329, L583, L693, L704 as of 2026-07-30 — L583 is the no-output re-submit handler; the last two shifted down 18 lines when the removal-retry filter was inserted; success paths use `<-`) · error handlers in `full_scm_search.R` · log accumulators in model-modification/recovery.

**Hardcoded relative paths:** `data/spec/tags.yaml`, `data/spec/lookup.yaml`, `models/scm_rds/`, `scenario_table_<model>.rds` (cwd), `results/figure/simulations/`, `scm_report.txt`.

---

## Package scaffolding

### R/CovariateSearchr.R
_Package-level scaffolding: `_PACKAGE` doc, the central `@importFrom` manifest, and the master `globalVariables` block. No functions._

**Notes:** No function definitions. Holds the central `utils::globalVariables(...)` block (~60 names: DB columns + dplyr-chain vars + per-function NSE vars for `extract_params`/`get_param2`/`model_report`/`submit_and_wait_for_step`) and the central `@importFrom` manifest on `"_PACKAGE"` (dplyr, tibble, bbr, yaml, readr, purrr, stringr, stats, utils, data.table, flextable, officer, tidyr, rlang). **This block is the single most important thing to keep intact when moving NSE-heavy functions.**

### R/imports.R
_Package import shell: the `%||%` null-coalescing operator and one stray import tag._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `` %||%(x, y) `` | ✓ | Null-coalesce: `x` unless NULL, else `y` | `x`, `y` | `x` or `y` | — | used throughout |

**Notes:** Only real code is `%||%`; carries a lone `#' @importFrom stats qchisq`. `%||%` is **re-defined locally** inside `generate_scm_report` (redundant shadow).

---

## Initialization & config

### R/initialization.R
_Main entry point: builds `search_state`, loads CSV/YAML inputs, validates, auto-generates `tags.yaml`, orchestrates DB/config/model-discovery setup._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `initialize_covariate_search(base_model_path, data_file_path, covariate_search_path, models_folder="models", timecol="TIME", idcol="ID", threads=60, validate_parameters=TRUE, require_base_run=TRUE, require_cov_step=TRUE, lookup_file=NULL, starting_model_number=NULL)` | ✓ | Full init: load data, verify ID col + `$DATA` filename match, gen tags, validate base model (skippable via `require_base_run=FALSE`), validate parameter transformations, build DB/config, discover models, set counter. **`require_cov_step` is the search-wide covariance-step rule set here**: passed to `validate_base_model_for_search()` so the base model is judged by it, and stored in `search_config` by `initialize_search_config()` **before** `discover_existing_models()` runs, which is what makes it reach discovery and every later status read | two CSV paths, base model name, `starting_model_number`, `require_cov_step` | full `search_state` list (12 slots: `base_model, data_file, covariate_search, models_folder, timecol, idcol, threads, tags, model_counter, search_database, search_config, discovered_models`) | `cat`; `readr::read_csv`; `readLines`; `dir.create` `models/scm_rds/`; many `stop()`; hardcoded `data/spec/tags.yaml` | calls `.load_if_path`, `generate_tags_from_covariate_search`, `validate_base_model_for_search`, `load_tags`, `validate_setup`, `initialize_search_database_core`, `initialize_search_config`, `discover_existing_models`, `update_model_counter`, `validate_base_model_parameters`; called-by `load_existing_search` |
| `load_tags(search_state)` | ✓ | Read `tags.yaml` into `$tags` | `search_state` | updated `search_state` | `yaml::read_yaml`; `stop()`; hardcoded `data/spec/tags.yaml` | called-by `initialize_covariate_search` |
| `validate_covariate_search_table(covariate_search, data_file)` | ✓ | Reusable checks: required cols, non-empty COV/PARAM, dup `cov_to_test`, covariates in data, categorical LEVELS/REFERENCE numeric & consistent, LEVELS numeric, **and `INIT` vs the FORMULA's theta count** (single-theta form ⇒ one plain estimate; multi-theta expression ⇒ one named entry per theta, names must match) | two data.frames | validated `covariate_search` (adds `cov_to_test`) | `cat`; many `stop()` | called-by `validate_setup`, `add_covariates_to_search` |
| `validate_setup(search_state)` | ✓ | High-level setup validation wrapper | `search_state` | updated `search_state` | `cat`; `stop()` | calls `validate_covariate_search_table`, `validate_covariate_parameter_mapping`, `validate_param_transformations`; called-by `initialize_covariate_search` |
| `validate_base_model_for_search(base_model_path, models_folder="models", require_cov_step=TRUE)` | ✓ | Confirm base model exists (`.ctl`/`.mod`), finished, has a readable OFV — held to the **same** completion rule as the search's own models, since `require_cov_step` is forwarded to `get_model_status_from_files()`. On any status other than `"completed"` it re-reads `.lst`/`.ext` to build a specific failure reason for the `stop()` message | model name, folder, `require_cov_step` | `TRUE` or `stop()` | `stop()` | calls `get_model_status_from_files`, `read_nonmem_lst`, `read_nonmem_ext`; called-by `initialize_covariate_search` |
| `initialize_search_config(search_state, lookup_file=NULL, require_cov_step=TRUE)` | ✓ | Set default SCM config (p-values, RSE via **`.DEFAULT_RSE_THRESHOLD`** — the same constant `.resolve_rse_threshold()` falls back to, so a new search and an older state cannot disagree on the default; threads, lookup, **`require_cov_step`**, phase) — 9 elements. Runs immediately before `discover_existing_models()`, so storing `require_cov_step` here is how the user's setting reaches discovery instead of a hard-coded TRUE | `search_state`, lookup, `require_cov_step` | updated `search_state` (`$search_config`) | `cat`; hardcoded `data/spec/lookup.yaml` | called-by `initialize_covariate_search`; `$search_config$require_cov_step` read by `discover_existing_models`, `update_model_status_from_files`, `run_automated_scm_testing` |
| `generate_tags_from_covariate_search(covariate_search, tags_yaml_path="data/spec/tags.yaml", verbose=TRUE)` | ✓ | Build/replace `## Covariates` section of `tags.yaml`; validates continuous FORMULA vocab | df or CSV path | logical | `writeLines`/`readLines`/`dir.create`; `stop()`; default `data/spec/tags.yaml` | nested `generate_tag_entry`; called-by `initialize_covariate_search`, `update_tags_yaml` |
| `update_tags_yaml(search_state=NULL, covariate_search=NULL, tags_yaml_path="data/spec/tags.yaml", verbose=TRUE)` | ✓ | Thin wrapper choosing df/path source | either arg | logical | `stop()` | calls `generate_tags_from_covariate_search` |
| `add_covariates_to_search(search_state, additions, tags_yaml_path="data/spec/tags.yaml", verbose=TRUE)` | ✓ | Extend a **live** search with covariates it did not start with: append rows, derive missing `cov_to_test`, revalidate the merged table (duplicate pair ⇒ `stop`), regenerate `tags.yaml`, refresh `$tags`. Reconciles column types so a CSV round-trip (numeric `REFERENCE`/`LEVELS`) still binds | `search_state`, new rows (df or `.csv` path) | updated `search_state` | rewrites `tags.yaml`; `stop()`/`warning` | calls `.load_if_path`, `validate_covariate_search_table`, `generate_tags_from_covariate_search`, `load_tags`; enables `add_covariate_to_model` on a covariate outside the original table |
| `build_covariate_reference_table(data, id, time, Parameter, Covariate, Category, Formula, yaml_data, INIT=NULL)` | ✓ | Build the `covariate_search` table from parallel vectors. Validates the spec (equal vector lengths, `STATUS` ∈ `con`/`cat`, one Category per covariate across rows, covariate/id/time columns present) and the FORMULA vocab (built-ins — con: `linear`/`power`/`exponential`, cat: `linear`/`power` — else an expression that parses **and** references `cov`). **REFERENCE comes from baseline only** (`Time == 0`, first row per ID): `round(median, 2)` for `con`, most-prevalent level for `cat`. **LEVELS comes from the FULL dataset, not baseline** — unique non-`NA` values of the whole column, so a level that only appears away from baseline (e.g. a time-varying dose under a `power` formula) is still captured; ordered to match `yaml_data[[cov]]$values` (falls back to lexicographic `sort()` only when the yaml lists no values), `NA` for `con`. Cross-checks those levels against the yaml, and derives TIME_DEPENDENT from the full data (>1 distinct value within a subject); optional `INIT` column | data, aligned spec vectors, `yaml_data` (needs `$<cov>$values` per categorical covariate — supplies both the legal set and the level ordering) | `data.frame` in spec-row order, columns PARAMETER, COVARIATE, STATUS, FORMULA, LEVELS, REFERENCE, TIME_DEPENDENT[, INIT] | `stop()` on spec/vocab errors, on a categorical covariate absent from `yaml_data`, and on a data level not defined in `yaml_data`; `warning()` when `yaml_data` defines a level never observed in the data, and `warning()` per time-dependent covariate flagging that its REFERENCE still reflects baseline only | calls `parse_covariate_expression`; nested `formula_ok`, `compute_ref_levels`, `compute_time_dependent`; feeds `initialize_covariate_search`; TIME_DEPENDENT drives covariate placement |
| `validate_param_transformations(covariate_search, data_file, id_col, model_name, models_folder="models")` | ✓ | Fail-fast guard for every **population** (time-constant) covariate target: parameterization must be classifiable as normal `P = TV*EXP(ETA)` or log `P = EXP(TV+ETA)`; a log target must have a separate `TV_<param>` line (inline `P = EXP(THETA+ETA)` rejected — the additive term would land outside the `EXP`); rejects disguised-log `TV_P = EXP(THETA(n))` and unbalanced parens | search table, data, model | `invisible(TRUE)` or `stop()` with a refactor message | `readLines`; `stop()` | calls `detect_param_transform`; called-by `initialize_covariate_search`, `prepare_search_base_model` |

**Notes:** `cov_to_test = beta_<COV>_<PARAM>` computed in two places here + in `validate_covariate_search_table` (dup). Continuous FORMULA vocab is now `linear`/`power`/`exponential` **plus user expressions** — `power1`/`power0.75` were removed (use `power` + `INIT` `"1 FIX"`/`"0.75 FIX"`). No schema/`action`/`phase` writes here.

---

## Database & state

### R/database-management.R
_Search-database schema/self-heal, RDS save/load, model-lookup accessors, model counter, comprehensive display-table builders._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `load_existing_search(base_model_path, data_file_path, covariate_search_path, models_folder="models", timecol="TIME", idcol="ID", threads=60, require_cov_step=TRUE, lookup_file=NULL)` | ✓ | Discovery-mode wrapper: re-init from existing models. `require_cov_step` is passed straight through to `initialize_covariate_search()`, so it governs both the base-model check and how every model found on disk is classified; `lookup_file` is applied *after* init by overwriting `$search_config$lookup_file`. **`require_cov_step` was inserted before `lookup_file`** — positional calls that used to pass the lookup 8th now set the covariance flag instead | CSV/model paths, `require_cov_step` | `search_state` or `stop()` | `cat`; `list.files`; `stop()` | calls `initialize_covariate_search` |
| `save_search_state(search_state, filename)` | ✓ | Save RDS; bare name → `models/scm_rds/` | state, filename | `invisible(search_state)` | `saveRDS`; `dir.create`; hardcoded `scm_rds/` | — |
| `load_search_state(filename)` | ✓ | Load RDS; the only migration is the provenance column (`.ensure_provenance_column`) so a pre-guard checkpoint gains `created_by_search` before anything appends to its DB | filename | `search_state` | `readRDS`; `cat`; `stop()` if the file is missing | calls `.ensure_provenance_column` |
| `initialize_search_database_core(search_state)` | ✓ | Create empty 19-col search DB (**canonical schema**) — 19th col is `created_by_search` (logical provenance flag) | `search_state` | updated `search_state` | `cat` | called-by `initialize_covariate_search`, `discover_existing_models` |
| `get_model_status(search_state, model_name)` | ✓ | DB status lookup | state, name | char status or `"not_found"` | — | — |
| `get_model_ofv_from_database(search_state, model_name)` | ✓ | Cached OFV lookup | state, name | numeric or `NA_real_` | — | — |
| `get_model_covariates_from_db(search_state, model_name)` | ✓ | Read **bbr tags**, intersect w/ `beta_*` tag **values** | state, name | char or `character(0)` | `bbr::read_model` | called widely (forward/backward/selective) |
| `update_model_counter(search_state)` | ✓ | Set `model_counter` to last non-retry run number | `search_state` | updated `search_state` | `cat` | called-by `initialize_covariate_search` |
| `create_comprehensive_table(search_state, use_separate_columns=TRUE)` | ✓ | Build display df (parent/type/step/changes/status/ofv/delta/param) | `search_state` | `data.frame` | `stop()` | calls `generate_step_display`/`_changes_display`/`_step_description`; called-by `view_comprehensive_table` |
| `generate_step_display(step_number, action)` | · | "Base"/"Step N"/"Step N (Retry)" | vectors | char vector | — | called-by `create_comprehensive_table` |
| `view_comprehensive_table(search_state, use_separate_columns=TRUE)` | ✓ | Print comprehensive table | `search_state` | `invisible(df)` | `cat`; `print` | calls `create_comprehensive_table` |
| `generate_changes_display(action, covariate_tested)` | · | "Add/Remove/Retry X" | vectors | char vector | — | called-by `create_comprehensive_table` |
| `generate_step_description(step_number, action, covariate_tested)` | · | "Step N: Add X" | vectors | char vector | — | called-by `create_comprehensive_table` |

**Notes:** **Canonical schema (19 cols):** `model_name, step_description, phase, step_number, parent_model, covariate_tested, action, ofv, delta_ofv, rse_max, status, tags (I(list())), submission_time, completion_time (POSIXct), retry_attempt, original_model, estimation_issue, excluded_from_step, created_by_search (logical)`. `created_by_search` is the provenance flag read by `.is_search_model`: `TRUE` for rows the package created (add/remove steps, retries, disk-reconstructed steps), `FALSE` for everything `discover_existing_models` found already on disk. Display generators branch on `action ∈ {base_model, add_covariate, remove_covariate, retry}` → discovered/manual/`*_single_*` rows fall through to "Unknown". No `action`/`phase` **written** here (display only).

### R/model-discovery.R
_Scans the models folder, parses bbr `based_on`/notes into DB rows, computes relative step numbers via parent-chain recursion._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `discover_existing_models(search_state)` | ✓ | Catalog `run\d+.(ctl|mod)`, read bbr per run, build `search_database`, assign relative step numbers. Every row it builds is stamped `created_by_search = FALSE` — it was on disk before the search started, so the search will not submit, retry or status-track it. Status per model comes from `get_model_status_from_files(model_path, require_cov_step = search_state$search_config$require_cov_step %||% TRUE)`, so a search initialized with `require_cov_step=FALSE` does not mark pre-existing runs `failed` for lacking a `.cov` (this works only because `initialize_search_config()` runs first) | `search_state` (needs `$search_config$require_cov_step`) | updated `search_state` (+ `$discovered_models`) | `cat`; `list.files`; `bbr::read_model`; `stop()` (missing base / circular parent) | calls `initialize_search_database_core`, `get_model_status_from_files`, `read_nonmem_ext`, `get_model_covariates_from_files`, `dplyr::bind_rows`; nested `get_step_number`, `is_descendant_of`; called-by `initialize_covariate_search` |

**Notes:** **4th row-builder** — hand-builds the 19-col row (must stay in sync w/ canonical schema), including `created_by_search = FALSE`. Writes `action ∈ {manual_modification(default), retry, add_single_covariate, remove_single_covariate}`, `phase ∈ {manual(default), retry, individual_testing}` — but then **overwrites** most: `phase <- ifelse(retry, phase, "manual")`, `action <- ifelse(retry, action, "manual_modification")`, so only `retry` survives; base row forced to `action="base_model"/phase="base"/step_number=0L`. Net: the note-parsing into `*_single_covariate` is discarded for non-retry models.

### R/validation.R
_Reads NONMEM output to update DB status/OFV/RSE/ΔOFV; THETA/OMEGA/SIGMA block-format validators; covariate→parameter mapping check._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `update_model_status_from_files(search_state, model_name, force=FALSE)` | ✓ | Read `.lst`/`.ext`/`.cov`, set status/ofv/rse_max/issue, compute signed ΔOFV vs parent (only when the model is `completed` with a non-NA OFV and the parent row exists with an OFV). **File resolution is delegated, not hardcoded:** the `.lst` comes from a single `read_nonmem_lst(model_path)` probe (so the run-dir *or* control-stream location is found), whose `$file` locates the listing **and** whose `$status` is checked first — a probe of `"read_error"` returns `failed` with the probe's own `error_message`, never `in_progress`, because the monitoring loop waits on `in_progress` and a file that will never become readable would hang it forever. It deliberately does **not** consume `$status == "failed"` — see the rejected-designs note in the hazards section. The `.ext` comes from a single `read_nonmem_ext(model_path)` call whose result is reused for OFV/parameters, and the `.cov` is looked for in `dirname(lst_file)` — wherever the rest of that run's output actually is. An absent/empty/unparseable `.ext` now reports `read_nonmem_ext()`'s own `$error` message (falling back to `"No EXT file found"` only when it is empty). **The ΔOFV sign is delegated to `.signed_delta_ofv(direction=NULL)`**, so this function no longer chooses a convention at all — each row's own `action` does, via `.is_removal_model()`: removal ⇒ `child − parent` (positive = OFV rose = the removal hurt ⇒ keep the covariate), anything else ⇒ `parent − child`. `NULL` is mandatory here: this is a back-fill over a mixed set of rows, so stamping one direction across it would mislabel the other kind. The helper also corrects a **retry of a removal** — its own `action` is `"retry"`, which used to fall to the forward branch and record the wrong sign | state, name, force; `$search_config$require_cov_step` (`%||% TRUE`) | updated `search_state` | `cat`; reads the listing via `.read_listing_lines` (latin1/`skipNul`, not bare `readLines` — only the bbi `OUTPUT` file is read with `readLines`); **self-heals missing DB cols** (13-col `required_cols` — note `action` is *not* in it, which `.is_removal_model` tolerates by returning FALSE); each missing column is filled with a scalar default of its own type via `if`/`else` — the previous `dplyr::case_when()` there **always threw**, since its branches mixed `NA_real_`/`FALSE`/`NA_character_` and `case_when` requires one shared type, so this recovery path crashed instead of recovering | nested `read_all_model_files`; calls `read_nonmem_lst`, `.read_listing_lines`, `read_nonmem_ext`, `extract_nonmem_timestamps`, `get_param2`, `.signed_delta_ofv`, `.resolve_rse_threshold` (the high-RSE warning is issued against the limit the search will actually judge the model by, so monitoring output and the selection decision cannot disagree); called-by `force_update_models`, `update_all_model_statuses` |
| `force_update_models(search_state, model_names)` | ✓ | Force-reread one/many | state, names | updated `search_state` | `cat` | calls `update_model_status_from_files` |
| `update_all_model_statuses(search_state, show_progress=TRUE)` | ✓ | Update all non-terminal models; report newly-significant via per-df thresholds. Three skip filters: pending status per `.is_pending_status()` (terminal = `completed`/`failed`/`estimation_error`), the bbr tag `do_not_run`, and — when the DB has the column — `created_by_search = FALSE`, **unless** some row names that model as its `parent_model` (step evaluation needs the parent's OFV for ΔOFV; the base model is the usual case). Rereading a foreign model each cycle would otherwise relabel a long-finished run as `failed` (no output dir reads as a failed launch). **The end-of-run significance report is direction-aware (2026-08-20):** it resolves *both* `forward_p_value` (`%||% 0.05`) and `backward_p_value` (`%||% 0.001`) from `search_config`, classifies each newly-completed row with `.is_removal_model()`, and tests that row's `delta_ofv` against `pvalue_to_threshold(<its own direction's p>, df)` — the df still coming from the row's `covariate_tested` tag (`extract_covariate_name_from_tag` → `calculate_covariate_df`, falling back to 1). The comparison stays `delta_ofv > threshold` for both directions because the two store opposite sign conventions, so the same positive number means "gain worth keeping" for an addition and "cost of dropping it" for a removal; only the heading differs. Removals therefore print under **`🔒 Removal rejected - covariate retained:`** with a ` (cost of removal)` suffix instead of being announced as improvements under `⭐ New significant improvements:` — the pre-2026-08-20 behaviour, which also judged them at the forward p-value | `search_state`; `$search_config$forward_p_value`/`$backward_p_value` | updated `search_state` | `cat` — progress lines, the `%d completed, %d failed total` summary, and (only under `show_progress`, only when something newly completed, and only when `model_name`/`delta_ofv`/`covariate_tested` are all present) the two significance headings, each group ordered by descending `delta_ofv`; the whole report is wrapped in a `tryCatch` that degrades to `⚠️  Could not generate significance summary` rather than failing the status update | calls `update_model_status_from_files`, `.is_pending_status`, `.is_search_model`, `.is_removal_model`, `extract_covariate_name_from_tag`, `calculate_covariate_df`, `pvalue_to_threshold`; called-by `submit_and_wait_for_step`, `select_best_model`, `continue_search`, `run_scm_selective_forward` |
| `get_model_max_rse(search_state, model_name)` | ✓ | Max finite RSE via `get_param2` | state, name | numeric or `NA_real_` | — | calls `get_param2` |
| `validate_parameter_blocks(model_file, check_omega_structure=TRUE, check_comments=TRUE, allow_empty_units=TRUE)` | ✓ | Check THETA/OMEGA/SIGMA line formatting | .ctl path | `list(valid, issues, warnings, model_file, blocks_checked)` | `readLines` | called-by `validate_base_model_parameters` |
| `print_parameter_validation(validation_result, verbose=TRUE)` | ✓ | Pretty-print validation + guidance | validation list | `invisible(...)` | `cat` | called-by `validate_base_model_parameters` |
| `validate_base_model_parameters(base_model_path, models_folder=NULL, strict=TRUE, check_omega_structure=TRUE, check_comments=TRUE)` | ✓ | Resolve base model, run+print block validation | model path/folder | validation `list` or `stop()` | `cat`; `stop`/`warning`; `list.files` | calls `validate_parameter_blocks`, `print_parameter_validation`; called-by `initialize_covariate_search` |
| `validate_covariate_parameter_mapping(covariate_search, model_name, models_folder="models", covariate_tags=NULL, strict=TRUE, verbose=TRUE)` | · | Verify each PARAMETER appears in model code | search table, model | `data.frame(cov_to_test, COVARIATE, PARAMETER, parameter_found)` | `cat`; `readLines`; `stop`/`warning` | calls `find_model_file`; called-by `validate_setup` |

**Notes:** **Self-heal schema #2** — `update_model_status_from_files` declares a 13-col `required_cols` subset, adds missing via `case_when` defaults (`NA_real_` for numerics, `FALSE` for `excluded_from_step`, else `NA_character_`). **Status values it writes:** `completed`/`failed`/`in_progress` only — its `results$status <- "unknown"` initial value and the internal `"stopped"`/`"read_error"` LST classifications never reach the DB (every return path overwrites them; `read_error` — from either the `read_nonmem_lst` probe or the re-read — becomes `failed`, matching `get_model_status_from_files`, and specifically **not** `in_progress`). **ΔOFV sign:** removal (per `.is_removal_model`) uses `child - parent`; everything else `parent - child` (positive=improvement both). Completion needs valid OFV AND (if `require_cov_step`) a `.cov` **in the directory the `.lst` was found in**; boundary params (`abs >= 8.99990e5`) → failed. This is deliberately the same test as `get_model_status_from_files()` — the two readers must agree, or a model changes verdict depending on which one looked.

---

## File I/O & model modification

### R/file-io.R
_NONMEM file I/O: locate/read/write control files; parse `.ext`/`.lst` for status, OFV, covariates, timestamps._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `read_model_file(search_state, run_name, extensions=c(".ctl",".mod"))` | ✓ | Read control file, stash path | `models_folder`+`run_name` | char lines w/ `file_path` attr | `stop()` if none | called-by `model_add_cov`, `remove_covariate_from_model` |
| `find_model_file(base_path, extensions=c(".ctl",".mod"))` | ✓ | Resolve actual model file path | `base_path` (no ext) | full path or `NULL` | — | called-by `adjust_theta_for_covariate`, validation, sim |
| `.read_listing_lines(path)` | · | **The one way to read a `.lst`** — plain `readLines()` is not safe on NONMEM listings. **Two-attempt read:** the session's own encoding first, so a valid UTF-8 listing keeps its non-ASCII characters in the text callers quote verbatim (`error_excerpt`, the `#TERM` block); on error, a `latin1` retry, where every byte maps to a character and nothing can throw (a run that emitted PRDERR output carries stray control bytes, which make `readLines()` throw *"invalid multibyte string"* under a UTF-8 locale). `skipNul=TRUE` on both, so embedded nuls are skipped instead of truncating the file. Status markers are ASCII and stay greppable either way | full path to an existing listing | `character()` of lines (`warn=FALSE`) | opens a connection per attempt, closed via `on.exit`; **errors propagate** — a path that cannot be opened at all fails both attempts and signals, so every caller wraps it in `tryCatch` | called-by `read_nonmem_lst`, `extract_nonmem_timestamps`, `update_model_status_from_files` (nested `read_all_model_files`) |
| `write_model_file(search_state, lines)` | ✓ | Write lines back to `file_path` attr | `lines` w/ attr | `search_state` | `writeLines`; `stop()` if attr missing | called-by `model_add_cov`, `remove_covariate_from_model` |
| `read_nonmem_ext(model_path)` | ✓ | OFV+params from `.ext` (last `-1e9` final-estimates line). **Fallback:** with no `-1e9` line it `warning()`s and parses the **last iteration line instead** — so a still-estimating run returns a usable-looking OFV, which is why status readers must confirm the run finished first | dir or `.ext` (3 candidates: `<path>/<name>.ext`, `dirname(<path>)/<name>.ext`, `<path>.ext`) | `list(found, file, ofv, parameters, n_parameters)` / `list(found=FALSE, error, ofv=NA, parameters=NULL)` — distinguishes not-found, "No data lines", and read error via `$error` | `warning()` | called-by `get_model_status_from_files`, validation, discovery, scm-algorithm, init |
| `read_nonmem_lst(model_path)` | ✓ | Classify `.lst` run status via regex (hard-failure markers beat success markers — a plain `any()` test, **not** order-aware; then `Stop Time:` + runtime-error/severe-warning markers, else `incomplete`). Its `$status` is a *listing* verdict, not the search's success test: no caller treats `"failed"` here as disqualifying, because success is a usable OFV plus a `.cov` (see the rejected-designs note in the hazards section); also **the resolver for where the output lives** — 3 candidates (`<path>/<name>.lst`, `dirname(<path>)/<name>.lst`, `<path>.lst`), so callers use `$file`/`dirname($file)` to find sibling output instead of assuming a layout. Reads through `.read_listing_lines()`, so a listing with non-UTF-8 bytes still classifies | dir or `.lst` | `list(found, file, status, error_message, error_excerpt, has_issues)`; status ∈ `not_run`/`incomplete`/`completed`/`failed`/`read_error`/`invalid_input`/`invalid_path`. **`$file` is present on every path where a listing was resolved — including the read-error handler, which returns `found=TRUE`, `status="read_error"` and the path.** Dropping `$file` there used to make an unreadable listing indistinguishable from one NONMEM has not written yet, so callers testing only `$file` read it as "still running". The three `found=FALSE` returns (`invalid_input`, `invalid_path`, `not_run`) carry no `$file` | — | calls `.read_listing_lines`; called-by `get_model_status_from_files`, `update_model_status_from_files`, `extract_nonmem_timestamps`, `validate_base_model_for_search` |
| `get_model_status_from_files(model_path, require_cov_step=TRUE)` | ✓ | Combine LST+EXT into one status — **the file-side definition of "did this run succeed"**, matching `update_model_status_from_files()` so both readers agree. Order matters: *finished* is established before any OFV is trusted, because `read_nonmem_ext()` falls back to the last iteration line, so a still-estimating model reports a usable-looking OFV. How minimisation *ended* is deliberately not part of the test — a run that stopped on rounding errors but produced an OFV and a `.cov` is a success | model path — a run dir *or* a control-stream base (either reader resolves both), **or the `.lst` itself**: a trailing `.lst` is stripped before either reader runs, because `read_nonmem_lst()` accepts the listing but `read_nonmem_ext()` would then hunt for `<model>.lst.ext`, find nothing, and make a finished run read as failed. Plus `require_cov_step` | char status: `"not_run"` (neither `.lst` nor `.ext` found) · `"incomplete"` (no `.lst`, or the `.lst`'s own status is `"incomplete"` — which includes an empty listing) · `"failed"` (the listing resolved but could not be read (`read_error`) — tested **before** the OFV, so an unreadable run can never fall through to success; **or** the `.cov` is missing under `require_cov_step`; **or** the `.lst` reports completion while the OFV is unusable — a missing/unparseable `.ext`, which `update_model_status_from_files()` also reports as failed) · `"completed"` (OFV non-NA, finite, `abs <= 1e10`; no parameter at `abs >= 8.9999e5`; and — when `require_cov_step` — the `.cov` exists, its **name and directory both taken from the resolved listing** (`dirname(lst_info$file)` + `basename(lst_info$file)` minus `.lst`), never from `basename(model_path)`, which would look for `<model>.lst.cov`) · otherwise the `.lst`'s own status. **No longer returns `completed_with_issues`** — that value is gone from the package. A `"completed"` verdict therefore always means both halves of the test were met; the `.lst` alone can never produce one | — | calls `read_nonmem_lst`, `read_nonmem_ext`; called-by `validate_base_model_for_search`, `discover_existing_models` |
| `get_model_covariates_from_files(search_state, model_name)` | ✓ | Covariate **tag names** present via bbr tags | `$tags` (`beta_*`) | char vec (names) or `character(0)` | `bbr::read_model` | called-by `discover_existing_models` |
| `extract_nonmem_timestamps(model_name, models_folder="models")` | ✓ | Parse start/stop from `.lst`. Locates the file via `read_nonmem_lst(...)$file` (run-dir *or* beside the control stream — no assumed `models/<run>/<run>.lst`) and reads it with `.read_listing_lines()`. **Parses under `LC_TIME="C"`**: the `%a`/`%b` in every candidate format are interpreted in the *reader's* locale, so on a non-English session none of the layouts matched and both timestamps came back `NA` with nothing signalled; the original locale is restored via `on.exit`. The stop marker is matched **unanchored** with `grep("Stop Time:", fixed = TRUE)`, the same way `update_model_status_from_files()` tests it, so a leading space cannot make one function call the run finished and the other not | name, folder | `list(start_time, stop_time, found_start, found_stop)`; on failure `list(start_time=NA, stop_time=NA, error=<"LST file not found"/"Empty LST file"/read error>)` (**no `found_*` fields on the error paths** — callers must not assume them) | **temporarily switches the session's `LC_TIME` to `"C"`** for the duration of the call (`on.exit` restore, both `suppressWarnings`) | calls `read_nonmem_lst`, `.read_listing_lines`; nested `parse_nonmem_time`; called-by `submit_and_wait_for_step`, `update_model_status_from_files` |
| `parse_nonmem_time(line)` | · nested | Parse one NONMEM timestamp line across host locales: builds two candidate strings (zone stripped mid-string `" [A-Z]{3,4} "`, zone stripped at end `"\\s+[A-Z]{3,4}\\s*$"`) and tries four formats against each — `%a %b %d %H:%M:%S %Y`, `%a %d %b %Y %I:%M:%S %p`, `%a %d %b %Y %H:%M:%S`, `%a %b %d %I:%M:%S %p %Y`. Each result is **tested** because `as.POSIXct()` returns NA on a format mismatch instead of signalling, so `tryCatch` would never fire. Correctness depends on the caller's `LC_TIME="C"` window — the day/month abbreviations NONMEM writes are English | one trimmed line | `POSIXct` (first format that parses) or `NA` (also for non-scalar/NA/empty input) | `suppressWarnings` only | nested in `extract_nonmem_timestamps`; used for both the first line (start) and the line after `Stop Time:` |

**Notes:** `read_nonmem_ext`'s `$parameters` **is** read now — both `get_model_status_from_files` and `update_model_status_from_files` scan it for NONMEM's boundary marker (`abs >= 8.9999e5`) before calling a run completed; `$n_parameters` is still read by no caller. `read_nonmem_lst`'s `$file` is likewise load-bearing: it is the single resolver for run-dir-vs-beside-the-`.ctl` output, and `get_model_status_from_files`, `update_model_status_from_files` and `extract_nonmem_timestamps` all anchor sibling files (`.cov`) and re-reads to `dirname()` of it rather than assuming a layout. `$file` is **absent only from the three `found=FALSE` returns** (`invalid_input`, `invalid_path`, `not_run`) — a listing that exists but cannot be read comes back `found=TRUE`, `status="read_error"` **with** `$file`, which is what lets a caller distinguish "unreadable" from "not written yet". Test `$status`, not just `$file`. Every read of a listing goes through `.read_listing_lines()` (latin1 + `skipNul`) — using `readLines()` directly on a `.lst` is the bug it exists to prevent. `read_model_file`'s `file_path` attr is a hard contract for `write_model_file`.

### R/model-modification.R
_Add/remove covariates by text-editing NONMEM control streams (THETA insert/renumber, FLAG-driven formulae) and appending `search_database` rows._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `add_covariate_to_model(search_state, base_model_id, covariate_tag, step_number=NULL, lookup_file=NULL, phase="forward_selection")` | ✓ | Copy parent, add one covariate, append DB row. Needs only two things from `search_state`: the tag in `$tags` and a matching `cov_to_test` row in `$covariate_search`. **`step_number` is REQUIRED** (stops if NULL — it is not auto-derived). `phase="individual_testing"` marks a deliberate one-off add outside the search | tag, base model | `list(status, model_name, covariate_added, step_number, search_state, technical_log, log_file)` / error list | `bbr::read_model/copy_model_from/add_tags/replace_all_notes`; `writeLines` logs; `log_filename <<-`; `stop()`; rolls back `model_counter` | calls `validate_covariate_parameter_mapping`, `model_add_cov`; called-by `run_univariate_step` |
| `.rhs_needs_paren_wrap(rhs)` | · | **The guard that decides whether wrapping is needed at all.** TRUE iff the RHS string contains a `+` or `-` at paren depth 0 — i.e. an appended `* factor` would bind only to the last term. FALSE for an already-flat multiplicative chain (`THETA(1) * (1 + (AGE-40)*THETA(5))`), so a second covariate add does **not** nest another paren layer around the first one and `strip_covariate_factors`' depth-0 scan can still see every factor. Character-by-character scan, no regex; a unary minus or an exponent (`1E-5`) at depth 0 returns TRUE, which only costs one harmless redundant paren pair | pre-comment RHS text (the caller strips `NAME =` and `; …` itself) | `TRUE`/`FALSE` | none — pure string function | called-by `model_add_cov` (gates the `.wrap_rhs_in_parens` call) |
| `.wrap_rhs_in_parens(line)` | · | Parenthesize the RHS of a `NAME = RHS [; comment]` control-stream line so a `* factor` appended after it binds to the **whole** expression, not just the last term of an existing sum (NONMEM/Fortran precedence: `A + B * f` ≠ `(A + B) * f`). Splits on the **first** `=` and the **first** `;`; keeps leading indentation, `trimws()`es the RHS, re-attaches the comment with four spaces. **Unconditional — it re-wraps whatever it is handed, so call it only behind `.rhs_needs_paren_wrap()`**; wrapping an already-multiplicative RHS pushes earlier covariate factors to paren depth > 0 where removal can no longer find them. A line with no `=` has its whole pre-comment text wrapped instead (`TV_CL ; c` → `(TV_CL)    ; c`) | one `$PK`/`$PRED` assignment line | the same line with `(...)` around its RHS | none — pure string function | called-by `model_add_cov` (multiplicative append whose RHS has a top-level `+`/`-`) |
| `model_add_cov(search_state, ref_model, cov_on_param, id_var="ID", data_file, covariate_search, capture_log=FALSE, lookup_file=NULL)` | ✓ | **Core control-stream editor:** picks placement (time-varying → individual/multiplicative, else population + `detect_param_transform` → mult/additive) and renders the effect from the registry (`nonmem` mult / `nonmem_log` additive), inserts N `$THETA` lines from `INIT`. **Placement is recomputed from `data_file`** (max unique levels of the covariate per `id_var`), **not** read from the table's `TIME_DEPENDENT` column. A multiplicative append rewrites the target line through `.wrap_rhs_in_parens` **only when `.rhs_needs_paren_wrap()` says the RHS has a top-level `+`/`-`**, so it cannot mis-bind to the last term of an existing sum yet also never nests parens on a flat multiplicative chain (which would hide earlier factors from removal); additive appends are never wrapped | `cov_on_param`, `data_file` | `list(search_state, log_entries)` or `search_state` | `write_model_file`; `stop()`; hardcoded `data/spec/lookup.yaml`; `captured_log <<-` | calls `get_covariate_formula`, `list_covariate_formulas` (error text), `detect_param_transform`, `.rhs_needs_paren_wrap`, `.wrap_rhs_in_parens`, `parse_named_init`, `read_model_file`, `write_model_file`; called-by `add_covariate_to_model`, `prepare_search_base_model` |
| `strip_covariate_factors(line, theta_nums)` | · | Remove every top-level `* <factor>` / `+ <term>` on a `$PK` line whose factor references one of `theta_nums` (balanced-paren aware), leaving the structural term and other covariates intact — one place for built-ins, user expressions and multi-theta forms | `$PK` line, THETA numbers | the line without those factors | — | called-by `remove_covariate_from_model` |
| `fix_theta_renumbering(modelcode, theta_numbers_to_remove, log_function)` | ✓ | Renumber `THETA(n)` after removals (temp placeholders) | modelcode, removed nums | char modelcode | — | called-by `remove_covariate_from_model` |
| `prepare_search_base_model(base_model_path, covariate_tags, new_model_number, data_file_path, covariate_search_path, models_folder="models", idcol="ID", overwrite=TRUE, lookup_file=NULL)` | ✓ | Build one child w/ many covariates + combined log (no DB row) | tags, paths | `list(status, model_name, model_path, parent_model, covariate_tags, covariates_added, log_file)` | `readr::read_csv`; `bbr::*`; `writeLines`; `stop()`; hardcoded lookup | calls `validate_covariate_parameter_mapping`, `model_add_cov` |
| `remove_covariate_from_model(search_state, model_name, covariate_tag, save_as_new_model=TRUE, step_number=NULL)` | ✓ | Strip covariate formula+THETA, renumber, append DB row. `step_number` is **required** (`stop()` if NULL). **Refuses to remove blind (2026-08-12):** after the Step-5 strip loop, `lines_modified == 0` while `theta_numbers_to_remove` is non-empty ⇒ `stop()` before Step 6, because deleting the `$THETA` line and renumbering when no `$PK` equation actually changed would silently rebind a surviving `THETA(n)` to the wrong parameter | tag, model | `list(status, model_name, covariate_removed, search_state)` | `bbr::copy_model_from/read_model/remove_tags/add_notes`; `write_model_file`; `writeLines` (`<model>_remove_<cov>_log.txt`, and `…_error_log.txt` from the `withCallingHandlers` error branch); `log_messages <<-`; `cat`; **`stop()` ×7** — NULL `step_number`, unknown tag, no matching `cov_to_test`, `bbr::copy_model_from` failure, no `$THETA` section, no THETA matched the covariate, and the new `lines_modified == 0` guard | calls `read_model_file`, `strip_covariate_factors`, `fix_theta_renumbering`, `write_model_file`; called-by `run_backward_elimination` |

**Notes — row-builders & FLAG logic (the reconciliation core):**
- **`add_covariate_to_model`** (`bind_rows`): `action="add_covariate"`, `phase` from the argument (default `"forward_selection"`), `step_number` **required** (`stop()` if NULL), `parent_model=base_model_id`, `covariate_tested=cov_to_test` (`beta_*`), `status="created"`, `tags=I(list(actual .yaml tags))`, `created_by_search=TRUE`, rest NA/0L/FALSE. Increments `model_counter`→`run<N>`; rolls back on error.
- **`remove_covariate_from_model`** (`bind_rows`, only if `save_as_new_model`): `action="remove_covariate"` (fixed 2026-07-18 at the root), `phase="covariate_removal"`, `parent_model=model_name`, `tags=I(list(character(0)))` (**discards** computed `remaining_tags`), `created_by_search=TRUE`.
- **`model_add_cov` rendering — the FLAG `case_when` is GONE (verified 2026-07-28: no `FLAG` in this file).** Dispatch is now the registry in `covariate-formula.R`: `get_covariate_formula(status, formula)` returns the entry, `$categorical` decides per-level `IF/ELSEIF` (cat.linear) vs a single factor, and `nonmem()` / `nonmem_log()` render multiplicative vs additive per the target's transform. `power1`/`power0.75` were removed — use `power` + `INIT` `"1 FIX"`/`"0.75 FIX"`. Init comes from the table's `INIT` column (per-theta named spec for multi-theta forms via `parse_named_init`), falling back to the entry's default. THETA lines: `beta_<COV>_<PARAM>` (1 theta), `beta_<COV>_<PARAM>_<THETANAME>` (multi-theta), `beta_<COV>_<PARAM>_<LABEL>` (categorical, decoded label — **not** the numeric level). Rewrites `$TABLE FILE=` to match run number. Removal goes through `strip_covariate_factors` + `fix_theta_renumbering`, so it no longer mirrors per-shape regexes. **Append safety:** a multiplicative add parenthesizes the existing RHS first, but **conditionally** — `.rhs_needs_paren_wrap()` gates `.wrap_rhs_in_parens()`, so a summed RHS is wrapped once and a flat multiplicative chain is left alone. The pairing is load-bearing in both directions: without the wrap the new factor mis-binds to the last term of a sum; with an *unconditional* wrap every earlier factor sinks below paren depth 0 and `strip_covariate_factors` can no longer remove it (see the struck-through "Parenthesized RHS hides earlier covariates from removal" entry under Confirmed bugs).

### R/covariate-formula.R
_Registry + source of truth for how a covariate effect is written into NONMEM: one entry per `(STATUS, FORMULA)`, parameter-transform detection, and user expression parsing. Consumed by the WRITE side (`model_add_cov`) and `calculate_covariate_df`. The forest READ side (`apply_covariate_model`) evaluates the model's own `$PK` and does **not** use this registry._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `register_covariate_formula(status, formula, nonmem=NULL, init="0.1", categorical=FALSE, nonmem_log=NULL)` | · | Store a form keyed `"status.formula"`. `nonmem(cova, ref, n)` renders the MULTIPLICATIVE factor for a normal-scale TV; `nonmem_log(cova, ref, n)` the ADDITIVE term for a log-scale TV; `categorical=TRUE` flags per-level `IF/ELSEIF` handling. **Internal since 2026-07-28** (`@keywords internal`/`@noRd`) — its only caller is this file, 5× at load time to declare the built-ins | status, formula, renderer fns | `invisible(key)` | `assign` into the load-time registry env | called at load time for the built-ins |
| `get_covariate_formula(status, formula)` | · | Look up an entry; **falls through to `parse_covariate_expression(formula)`** when the name isn't registered, so a raw expression works like a built-in | status, formula | entry `list` or `NULL` | — | called-by `model_add_cov`, `calculate_covariate_df` |
| `list_covariate_formulas()` | · | The registered `"status.formula"` keys. **Internal since 2026-07-28** | — | sorted `character` | — | called-by `model_add_cov` (the "unknown formula" error message) |
| `detect_param_transform(modelcode, param)` | · | Classify a parameter: `"normal"` (`P = TV*EXP(ETA)`), `"log"` (`P = EXP(TV+ETA)`), `"unknown"` (ETA not inside an `EXP` → exotic, callers reject). No IIV on the parameter ⇒ `"normal"`. Robust to models that already carry covariates. **MU-referencing is disambiguated by the `$THETA` tag (2026-08-11):** the tag is consulted only when **two** conditions both hold — (1) the leftover typical-value symbol riding inside the `EXP` alongside `ETA` has its own line `<sym> = LOG(TV_<param>)` (case-insensitive, and `TV_<param>` specifically), and (2) `has_tv_line`: a line actually *starts* with that TV symbol, `^\s*TV_<param>\b` (case-sensitive; the `=` is not part of the pattern). That second pattern is byte-identical to the one `model_add_cov` uses to locate its write target (model-modification.R:556-557) and to `validate_param_transformations`' no-`TV_` check (initialization.R:932), which is the point: the tag may only override when there is a TV line the writer can actually reach. When both hold the form is algebraically `TV*EXP(ETA)`, so the parameter's `; <PARAM> ; units ; RATIO\|LOG` tag decides: `RATIO` ⇒ `"normal"`, `LOG` ⇒ `"log"`. **No tag ⇒ the old `"log"` default**, so an untagged MU model (i.e. one never run through `validate_parameter_blocks`) still classifies as before. Everything failing either condition stays `"log"`: a MU reference to something *other* than `TV_<param>` (e.g. `MU_1 = LOG(THETA(1))`, no `TV_` line at all), and — the case condition (2) closes — a **conditionally assigned** TV that only ever appears mid-line (`IF(SEX.EQ.1) TV_CL = …`, never at line start). Both then still trip `validate_param_transformations`' no-`TV_` `stop()` instead of being reclassified `"normal"`, which would have sent `model_add_cov` down its fallback and written the covariate onto the `PARAM = EXP(MU+ETA)` line, destroying the MU form | **whole control stream** (the tag lookup needs the `$THETA` block, not just the `$PK` line), param name | `character(1)` ∈ `normal`/`log`/`unknown` | — | calls `.exp_inner_containing_eta`, `.theta_tag_for`; called-by `model_add_cov`, `validate_param_transformations` |
| `.exp_inner_containing_eta(rhs)` | · | Body of the first paren-balanced `EXP(...)` whose content holds an `ETA(` (`\b` guard so `THETA(` doesn't match); `NA` if the ETA isn't wrapped in an `EXP` | RHS of the parameter line | `character(1)` or `NA` | — | called-by `detect_param_transform` |
| `.theta_tag_for(modelcode, param)` | · | Exact-name lookup of the `value ; NAME ; units ; RATIO\|LOG` comment tag that `validate_parameter_blocks` requires on every `$THETA`/`$OMEGA`/`$SIGMA` line — the tie-breaker for MU-referenced parameters. Scans **every line containing a `;`**, not only `$THETA` lines: strips the text before the first `;`, splits the rest on `;`, drops empty fields, and requires ≥2 fields whose **first** is exactly `param` (case-sensitive `identical`); the **last** field, uppercased, is the tag. First line yielding `RATIO`/`LOG` wins; a matching line with any other trailing field is skipped and the scan continues | model lines, exact parameter name | `"RATIO"`, `"LOG"`, or `NA_character_` when no such line exists | — | called-by `detect_param_transform` |
| `parse_covariate_expression(formula)` | · | Treat a non-built-in FORMULA as a single-factor expression in reserved `cov` (and optional `ref`); **every other symbol is an estimated THETA**, in order of appearance. Rejects anything using a function outside `.COV_EXPR_ALLOWED` (`+ - * / ^ exp log log10 sqrt`) — notably `if`/`ifelse`, which keeps expressions single-factor | formula string | entry `list(expr, theta_names, nonmem, nonmem_log, ...)` or `NULL`. **`nonmem_log` is the same rendering joined with `+` instead of `*`** — the expression is never transformed | — | `all.vars`/`all.names`; called-by `get_covariate_formula` |
| `.translate_expr_to_nonmem(expr, cova, ref, thetas, n)` | · | AST walk rendering the R expression as NONMEM: `cov`→covariate column, `ref`→its value, parameter symbols→`THETA(n)`, `THETA(n+1)`…, math fns uppercased, `^`→`**` | parsed expr | `character(1)` | — | called-by the expression entry's `nonmem()` |
| `parse_named_init(init_str, theta_names)` | · | Split a `;`-delimited **named** INIT spec (`"EMAX=0.1; EC50=(0,10,1000)"`) into one `$THETA` init string per theta, aligned to `theta_names`, defaulting to `"0.1"`; order-independent, unknown names ignored | INIT cell, theta names | `character(length(theta_names))` | — | called-by `model_add_cov` (multi-theta path) |
| `.cov_pow_nonmem(cova, ref, n)` | · | Multiplicative power renderer: `* (COV/ref)**THETA(n)` | cova, ref, n | `character(1)` | — | registered for both `con.power` and `cat.power` |
| `.cov_pow_logadd(cova, ref, n)` | · | Additive (log-scale) power renderer: `+ THETA(n)*LOG(COV/ref)` | cova, ref, n | `character(1)` | — | registered for both `con.power` and `cat.power` |

**Registered built-ins** (byte-exact mirror of the legacy output):

| key | normal (`*` factor) | log (`+` term) |
|---|---|---|
| `con.power` | `* (COV/ref)**THETA(n)` | `+ THETA(n)*LOG(COV/ref)` |
| `con.linear` | `* (1 + (COV-ref) * THETA(n))` | `+ LOG(1 + (COV-ref) * THETA(n))` |
| `con.exponential` | `* EXP(THETA(n) * (COV-ref))` | `+ THETA(n) * (COV-ref)` |
| `cat.power` | same as `con.power` — a **numeric-levelled** covariate (e.g. dose 35/70/125/150) as a power relation; **not** categorical, so no `IF` block | same |
| `cat.linear` | — | — (flagged `categorical=TRUE`; the per-level `$PK` `IF/ELSEIF` block stays in `model_add_cov`) |

**Notes:** `power1`/`power0.75` were **removed** — express them as `power` + `INIT` `"1 FIX"` / `"0.75 FIX"`. **A user expression now carries a `nonmem_log` too (2026-07-28): the same string joined with `+` instead of `*`.** So on a log-scale population parameter the expression is ADDED, not multiplied, and it is inserted *verbatim* — writing it on the log scale (e.g. `log(EMAX*cov/(EC50+cov))`) is the user's responsibility, flagged by a WARNING. The package deliberately does NOT wrap it in `LOG()` for you. The registry's old read-side `r_eval` fields are gone — `apply_covariate_model` evaluates the `$PK` equations instead, so write and read cannot drift.

**Transform detection depends on the parameter-block tags (2026-08-11).** A MU-referenced model (`MU_1 = LOG(TV_CL)`; `CL = EXP(MU_1 + ETA(1))`) is structurally indistinguishable from a genuine log parameterization — a typical-value term riding inside the `EXP` next to `ETA` — but is algebraically `TV*EXP(ETA)`, i.e. *normal* space, so writing the covariate additively there would be wrong. `detect_param_transform` therefore falls back to the `; <PARAM> ; units ; RATIO|LOG` tag (`.theta_tag_for`) for that one shape. Two consequences: (1) the classification of a MU model now depends on a **comment**, so a mis-tagged `$THETA` line silently changes where `model_add_cov` writes the covariate — `validate_parameter_blocks` (run when `initialize_covariate_search(validate_parameters = TRUE)`) is what makes the tag trustworthy, and an untagged MU model still classifies as `"log"` exactly as before; (2) the rule is safe only because `validate_param_transformations` already `stop()`s on the disguised-log form `TV_<param> = EXP(THETA(n))`, so a `RATIO`-tagged TV cannot itself be on the log scale. Those two functions must stay paired. **Narrowed 2026-08-12:** the tag override additionally requires the TV line to exist as a line-start `^\s*TV_<param>\b` assignment (`has_tv_line`) — the same pattern the writer and the no-`TV_` check use — so a TV assigned only inside an `IF` block is no longer eligible for reclassification and reaches the existing `stop()` instead.

### R/monitoring-files.R
_Single-model `.ext` parser: live OFV/iteration status + estimation-issue detection._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `read_ext_file(search_state, model_name)` | ✓ | Parse last data line of final `.ext` table; detect issues | `models_folder`+`model_name` | `list(status, current_ofv, iterations, last_iteration_time, has_estimation_issues, issue_type, estimation_method)` | — | called-by `detect_estimation_problems` |

**Notes:** Expects `.ext` at `models_folder/<m>/<m>.ext` (subdir only), unlike `read_nonmem_ext` (flat+subdir). OFV = **last column of last line** (monitoring "current"); issue threshold `abs(ofv) > 1e10`; `issue_type ∈ {infinite/nan/high/missing_ofv, problematic_parameters, parse_error, read_error:*}`.

---

## Recovery

### R/recovery-detection.R
_Scan models for estimation problems (via `.ext`) and produce an aggregate recovery report._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `detect_estimation_problems(search_state, model_names, check_interval_minutes=30)` | ✓ | Check each model's `.ext`; flag DB rows | `model_names` | `list(search_state, models_with_issues)` | mutates `estimation_issue`/`status="estimation_error"`; `cat` | calls `read_ext_file` |
| `generate_recovery_report(search_state)` | ✓ | Summarize status distribution + retry stats | `search_database` | `list(timestamp, status_distribution, retry_statistics, excluded_covariates, recovery_success_rate)` | — | calls `get_excluded_covariates` |

**Notes:** `check_interval_minutes` unused (dead param). Retry detection `grepl("\\d{3}$")` misclassifies `run100` etc.

### R/recovery-actions.R
_Retry/exclusion recovery: build `run<N>001` retries from a perturbed covariate THETA init, route retries vs exclusions, mark failed covariates._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `create_retry_model(search_state, original_model_name, issue_type="estimation_error")` | ✓ | Copy failed model → `run<num>001`, perturb THETA init, append DB row. **Pre-checks with `dry_run=TRUE` on the ORIGINAL before copying: all-FIX ⇒ returns `status="skipped"`, no files/DB row.** Calls `.ensure_provenance_column` first so its `rbind` template already carries every column, then sets `created_by_search=TRUE` explicitly (the blanked template is copied from `db[1,]`, usually a discovered row) | original name | `list(status = "created"/"skipped"/"failed", ...)` | `bbr::copy_model_from/read_model/replace_all_notes`; sets `based_on`; `writeLines`; `log_entries <<-`; `stop()` | calls `.ensure_provenance_column`, `adjust_theta_for_covariate`; called-by `process_estimation_issues` |
| `adjust_theta_for_covariate(search_state, model_name, covariate_tag, dry_run=FALSE)` | ✓ | Perturb the covariate's THETA init(s) — see rules in Notes | tag, model | `list(success, reason, theta_lines_modified, fixed_lines_skipped, covariate, matched_terms)` | `writeLines` model file (not when `dry_run`); `cat` | calls `find_model_file`, `.perturb_theta_init`, `.theta_init_is_fixed`; called-by `create_retry_model` |
| `.theta_init_is_fixed(spec)` | · | Is this init FIXED? Reads the spec only (text before the first `;`), so a `FIX` in a comment can't trigger it | one `$THETA` init spec | logical | — | called-by `adjust_theta_for_covariate` |
| `.perturb_theta_init(spec)` | · | Apply the perturbation rules (see Notes); `NA` when there is nothing to move to (unparseable, or bounds with no room) | one `$THETA` init spec | new spec or `NA` | — | called-by `adjust_theta_for_covariate` |
| `.format_theta_init_value(value, template)` | · | Render the new value at the precision of the value it replaces, widening only when that would lose information (midpoint `0.05` replacing `0.1`) | new value, old value string | `character(1)` | — | called-by `.perturb_theta_init` |
| `process_estimation_issues(search_state, models_with_issues)` | ✓ | Route each issue: retry originals / exclude failed retries | `models_with_issues` | `list(search_state, retry_models_created, excluded_covariates, recovery_actions)` | `cat` | calls `create_retry_model`, `handle_failed_retry`; called-by `submit_and_wait_for_step` |
| `handle_failed_retry(search_state, retry_model_name, exclusion_reason="retry_failed")` | ✓ | Mark retry+original excluded from step | retry name | `list(search_state, status, excluded_covariate, ...)` | mutates several DB cols; `cat` | called-by `process_estimation_issues` |

**Notes:** **3rd row-builder** — `create_retry_model` uses **template `db[1,]` + blank + `rbind`** (order-sensitive, not `bind_rows`): `action="retry"`, `phase="retry"`, `retry_attempt=1L`, `original_model=original`, `estimation_issue=issue_type`, `created_by_search=TRUE`. Because the `rbind` is width-sensitive it calls `.ensure_provenance_column(search_state)` before taking the template. Sets bbr `based_on` to the original **parent** (skip failed).

**THETA perturbation rules (2026-07-28)** — the init comes verbatim from the covariate table's `INIT` column, so it can be bounded or FIX. Parsed from the text **before the first `;`** (so a `FIX` in a comment can't trigger it):

| init form | new init | note |
|---|---|---|
| `0.1` | `-0.1` | sign flip, the long-standing rule |
| `(0, 0.1, 3)` | `(0, 1.55, 3)` | midpoint of the **wider side**; always strictly inside the bounds |
| `(0, 0.1)` | `(0, 0.05)` | one-sided: flip if still legal, else midpoint of the bounded side |
| `0.75 FIX` | untouched | a fixed value is a modelling choice, not a starting point |

Zero perturbable lines: `reason="all_thetas_fixed"` (≥1 matched line, all FIX) ⇒ **`create_retry_model` skips the retry entirely**; `reason="no_theta_lines"` (nothing matched) still creates the retry — an identical rerun is legitimate recovery for a Metworx cluster drop. `process_estimation_issues` maps the skip to `action="retry_skipped"` (not a failure) and writes `SKIPPED_retry_<model>_log.txt`. Multi-theta/categorical: FIX and non-FIX thetas of the same covariate are handled per-line, so a partly-FIX covariate still retries.

---

## SCM engine

### R/full_scm_search.R
_Top-level orchestrator: (optional initial backward →) forward → final backward → excluded-covariate final testing, with checkpoints + reporting._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `run_automated_scm_testing(search_state, base_model_id=NULL, scm_type=c("standard","selective"), starting_phase=c("forward","backward"), full_scm=TRUE, forward_p_value=NULL, backward_p_value=NULL, rse_threshold=NULL, require_cov_step=TRUE, auto_submit=TRUE, auto_retry=TRUE, save_checkpoints=TRUE, final_testing=TRUE)` | ✓ | Drive the whole SCM/SCM+ pipeline; dispatch standard vs selective forward + backward. Writes `forward_p_value`/`backward_p_value`/`max_rse_threshold` into `search_config` unconditionally (NULL ⇒ keep the configured value), with the RSE limit resolved through `.resolve_rse_threshold()` rather than an inline `%||% 50`, but **`require_cov_step` is only written when the caller actually supplied it** (`if (missing(require_cov_step)) require_cov_step <- search_state$search_config$require_cov_step %||% TRUE`), so a FALSE chosen at `initialize_covariate_search()` survives this argument's TRUE default. Validated with `if (!is.logical(require_cov_step)) stop(...)` | `scm_type ∈ {standard,selective}`, `starting_phase ∈ {forward,backward}`, `full_scm`, p-values, `rse_threshold`, optional `require_cov_step` | big `list(search_state, status, ..., final_model, forward_results, initial_backward_results, final_backward_results, final_covariates, excluded_covariates, ..., final_summary)` | `save_search_state()`→`scm_rds/NN_phase_event.rds`; `stop()`; `<<-` in 3 error handlers; heavy `cat` | calls `.resolve_rse_threshold`, `pvalue_to_threshold`, `run_backward_elimination`, `run_stepwise_covariate_modeling`, `run_scm_selective_forward`, `run_univariate_step`, `submit_and_wait_for_step`, `select_best_model`, `get_model_covariates_from_db`, `get_excluded_covariates`, `save_search_state`; **top-level (no in-repo callers)** |

**Notes:** Phase flags derived from `full_scm`+`starting_phase`. Reconciles selective vs standard via `final_best_model %||% final_model`. **Return shape is load-bearing** (`create_scm_results_table` unwraps `$search_state`, reads `forward_results`/`final_backward_results`/`excluded_covariates`/`final_summary`). Excluded-covariate final-testing block is **dead** (only fires `run_forward && !run_final_backward`, false for `full_scm`; also filters `phase=="forward"` never written). Stale "backward not implemented" comments; mojibake banner at L318.

### R/scm-algorithm.R
_Standard forward SCM + covariate-set helpers + exclusion-status printer._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `get_remaining_covariates(search_state, base_model_id, include_excluded=TRUE)` | ✓ | `^beta_` tags not yet in model, optionally minus excluded | state, base, flag | `character()` tags | `cat` | calls `get_model_covariates_from_db`, `get_excluded_covariates`; called-by forward drivers |
| `get_excluded_covariates(search_state, return_details=FALSE, phase_filter=NULL)` | ✓ | Covariates flagged `excluded_from_step==TRUE` | flags | `character()` or details `data.frame` | — | called-by `get_remaining_covariates`, `run_univariate_step`, `view_exclusion_status`, orchestrator |
| `run_stepwise_covariate_modeling(search_state, base_model_id=NULL, auto_submit=TRUE, forward_p_value=NULL, rse_threshold=NULL)` | ✓ | **Standard forward SCM:** univariate step 1 then iterative forward until no improvement. DB-driven each step (`get_remaining_covariates(current_base)`), so resume-safe by passing the current best as `base_model_id`. | base, p-value, rse | `list(search_state, status, base_model, final_model, steps_completed, step_results, total_time_minutes, final_covariates)` | reads `.ext`/`.yaml`; `save_search_state()`→`scm_forward_step_<N>.rds` after each step; `cat` | calls `get_remaining_covariates`, `run_univariate_step`, `submit_and_wait_for_step`, `select_best_model`, `get_model_covariates_from_db`, `read_nonmem_ext`, `pvalue_to_threshold`, `save_search_state`; called-by orchestrator + `continue_search` |
| `view_exclusion_status(search_state)` | ✓ | Pretty-print exclusion table | `search_state` | `invisible(NULL)` | `cat` | calls `get_excluded_covariates` |

**Notes:** Roxygen advertises "final testing of dropped covariates" but body only does forward steps (that phase is in the orchestrator). Step numbers derived from `max(step_number)+1`.

### R/scm-execution.R
_One univariate SCM step (create candidates) + submit-and-monitor loop with auto-retry._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `run_univariate_step(search_state, base_model_id, covariates_to_test=NULL, step_name, include_excluded=TRUE)` | ✓ | One child model per covariate tag at a shared step number | tags, step_name | `list(search_state, ..., models_created, successful/failed_covariates, status)` | `stop()` on invalid tags / step inconsistency; `cat` | calls `get_remaining_covariates`, `get_excluded_covariates`, `add_covariate_to_model`; called-by forward drivers + orchestrator |
| `submit_and_wait_for_step(search_state, model_names, step_name, max_wait_minutes=NULL, threads=NULL, auto_submit=TRUE, auto_retry=TRUE)` | ✓ | Submit via bbr, poll 60s, auto-retry failures until done. **Provenance-gated:** names failing `.is_search_model` are printed and dropped before submission (nothing left ⇒ returns `status="no_models"`), and a retry model is re-checked before *its* submission. Completion/failure decisions are scoped to `active_monitoring_list` (this step's models + its retries) even though the printed summary still covers the whole DB. A newly-failed model whose output dir does not exist is **re-submitted once** (tracked in `resubmitted_never_started`) instead of going down the retry path — its inits are not what stopped it; a second no-output result falls through to the normal failure path. **A failed *removal* model gets no retry at all:** `.is_removal_model` picks those out of `newly_failed` immediately before the auto-retry block, so backward elimination never creates a `<name>001` model. Status is locked to `"failed"` and the name is added to `definitively_failed` *before* that filter, so they stay reported and are not re-picked next cycle — a removal that will not estimate just means the covariate stays, which is what `evaluate_removal_impacts()` concludes from it not completing. A removal model that *never launched* is a different case and is caught by the no-output re-submit above instead | `model_names` | `list(search_state, completed_models, failed_models, still_running, total_time_minutes, timed_out, updates_performed, retry_models_created, status)` | **`bbr::read_model/submit_model`** (initial, re-submit, retry); `save_search_state()`→`scm_rds/NN_<phase>_running.rds` every 5th cycle; `Sys.sleep(60)`; `dir.exists`; **`<<-` in error handlers** (L323-324, L329, L583, L693, L704); `cat` | calls `.is_search_model`, `.is_removal_model`, `bbr::*`, `find_model_file`, `update_all_model_statuses`, `extract_nonmem_timestamps`, `process_estimation_issues`, `save_search_state`, `.scm_checkpoint_name`; called-by all drivers |

**Notes:** `run_univariate_step`'s tryCatch error handler records failures with `<<-` (L153-154) — the handler-frame `<-` bug was fixed 2026-07-18. `submit_and_wait_for_step` = the long-running monitor (`while(TRUE)` until `models_still_running==0`/timeout); tracks completed/failed/already-retried to fire `<failed>001` retries once, and separately tracks `resubmitted_never_started` so a no-output model is re-submitted at most once. Removal failures are dropped from `newly_failed` before the retry block (`.is_removal_model`) and so are deliberately *not* added to `already_processed_for_retry` — `definitively_failed` is what keeps them from being re-picked. **`<<-` at L323-324, L329, L583, L693, L704 mutate enclosing locals (`failed_submissions`, `submission_results`, `already_processed_for_retry`) and `search_state$search_database$status` from inside `tryCatch` error callbacks — must return state if extracted.**

### R/scm-evaluation.R
_The one acceptance test (`.evaluate_model_criteria`) every phase and caller judges a model by, plus the "pick a model" helpers built on it._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `.evaluate_model_criteria(search_state, model_names, phase=c("forward", "backward"), p_value=NULL, rse_threshold=NULL)` | · | **THE single answer to "may the search use this model"** — status `completed` AND ΔOFV clearing the χ² threshold for *that covariate's own* df AND the RSE test, which is now **`require_cov_step`-dependent**: default TRUE rejects an unreadable `rse_max`, FALSE accepts it (see Notes). `phase` is not just a different p-value: forward and backward store `delta_ofv` with **opposite sign conventions** (fwd `parent − model`, positive = better; bwd `model − base`, positive = worse) *and* compare it in **opposite directions** (`>` vs `<`), so the phase selects both. RSE test is identical in both, but what passing it authorises differs (fwd accepts the covariate, bwd accepts its **removal**). Reads the DB only — the caller must have written `delta_ofv` to the row first | `model_names`, `phase`, p-value (defaults to the phase's config), rse | `data.frame`, one row per name **found in the DB**, in the order given: `model_name, status, ofv, delta_ofv, rse_max, covariate_df, ofv_threshold, meets_ofv, meets_rse, meets_threshold, note`; the same 11-col frame with 0 rows when the DB is empty/no name matches. The column names are the ones `evaluate_removal_impacts()` reports, so its result frame is assembled straight from these | **none** — pure read, no `cat`/`stop`/write. A missing DB column reads as all-`NA` rather than erroring (a checkpoint predating `rse_max` still evaluates — on OFV alone only when `require_cov_step=FALSE`; under the default it now fails the RSE test); tag/df lookup errors are swallowed to `df=1`. **`meets_ofv`/`meets_rse`/`meets_threshold` are NA-coerced to `FALSE`** before returning: they are the contract every consumer indexes on, and `x[NA]` in R *returns* `NA` rather than dropping the element, so one `NA` verdict became an `NA` **model name** out of `get_significant_models_from_step` — a model that does not exist. Fails closed | calls `.resolve_rse_threshold`, `extract_covariate_name_from_tag`, `calculate_covariate_df`, `pvalue_to_threshold`; called-by `select_best_model`, `.best_acceptable_model`, `get_significant_models_from_step`, `evaluate_removal_impacts`, nested `get_selection_info` (scm-results.R) |
| `.best_acceptable_model(search_state, model_names, p_value=NULL, rse_threshold=NULL)` | · | Lowest-**OFV** model *among those meeting the criteria*. Backs every "which model do we carry forward" decision that is not a single step's winner — the redemption re-base, the post-redemption overall pick, and the fallbacks either side. Those sites used to take `which.min(ofv)` over completed models with **no acceptance test at all**, which is how a model failing the RSE limit could become the base for backward elimination purely on fit. Judges with `phase="forward"` (hardwired). **Contract: the base model is never returned** — it carries no `delta_ofv`, so it cannot pass `meets_ofv`. It is the *incumbent*, not a candidate; a caller needing a fallback when nothing qualifies must name one itself | candidate `model_names`, p-value, rse | `list(model, rejected)` — `model` is `NULL` when nothing qualifies; `rejected` = `status %in% "completed" & meets_ofv & !meets_rse & !is.na(ofv)`, i.e. **only completed** models that had the fit to win and lost on RSE, so a model excluded for some other reason is not announced as an RSE rejection | none | calls `.evaluate_model_criteria`; called-by `run_scm_selective_forward` (×4: fallback base, redemption-base fallback, per-redemption-step re-base, overall best) |
| `.report_rse_rejections(rejected, context)` | · | Name the models that had the fit to win but failed the RSE limit, so an RSE-driven choice is legible in the log at the time it is made instead of only in a later report | the `rejected` element of `.best_acceptable_model()`, a context string | `invisible(NULL)` | `cat` (one ⚠️ line per rejected model) | called-by `run_scm_selective_forward` (×4, paired with each `.best_acceptable_model` call) |
| `select_best_model(search_state, model_names, p_value=NULL, rse_threshold=NULL)` | ✓ | Pick the highest-ΔOFV model clearing its df-specific χ² threshold + RSE. Re-reads outputs, back-fills `delta_ofv` from the parent where missing — through **`.signed_delta_ofv(direction="forward")`**, stated rather than derived because this function also fixes the test's direction (`phase="forward"` below); deriving would store a removal as `model − parent` and then test that same row with `delta_ofv > threshold`, so a removal costing 70 OFV points would read as a 70-point improvement and be **selected**. **`warning()`s** when handed a removal model, since that means the caller assembled `model_names` wrongly — **then delegates the verdict to `.evaluate_model_criteria(phase="forward")`** and copies its columns onto the local frame as `covariate_df`/`ofv_threshold`/`delta_ofv_significant`/`rse_acceptable`/`overall_significant`/`evaluation_notes` | `model_names`, p-value, rse | `list(search_state, best_model, significant_models, evaluation_results, criteria_used, status)`; `status="no_completed_models"` short-circuit when nothing completed | mutates `delta_ofv` in **both** the local frame and `search_state$search_database` for rows missing it (this is what the evaluator then reads back); `cat` | calls `update_all_model_statuses`, `.resolve_rse_threshold`, `.signed_delta_ofv`, `.evaluate_model_criteria`, `pvalue_to_threshold` (display line only); called-by forward drivers + orchestrator |

**Notes:** Threshold per-model from covariate df (default df=1 when unresolved). **The "`NA` RSE is acceptable everywhere" rule is retired (2026-08-07)** — it held for the whole earlier life of this file, so discard any memory of it. `meets_rse` now follows `search_config$require_cov_step` (`%||% TRUE`), and **the test is `identical(require_cov, FALSE)`, not `isTRUE(require_cov)`** — only an explicit `FALSE` relaxes the rule; `NA`, `"yes"`, a vector, or a missing `search_config` are all **strict**. The `isTRUE` form failed *open*: every malformed value fell through to the lenient branch and silently switched the RSE criterion off, which is the opposite of `.resolve_rse_threshold`'s treatment of a bad threshold. `initialize_search_config()` stores whatever it is handed, so the guard has to live here. So: default/strict ⇒ `!is.na(rse_max) & rse_max < limit`, **an unreadable RSE REJECTS the model**; explicit `FALSE` ⇒ the old `is.na(rse_max) | rse_max < limit`. The two settings are one question asked twice — a search that demands a covariance step cannot accept a model whose precision it could not read, and one that does not demand it must not reject a model for lacking something it never asked for. An unknown RSE prints as `"NA"`, never `0%`, and its note reads "RSE unavailable (a successful covariance step is required)" rather than `NA% > 50%`, which would hide *which* of the two failures happened. `select_best_model`'s best = `which.max(delta_ofv)` among significant rows (fit-ranked), while `.best_acceptable_model`'s best = `which.min(ofv)` among acceptable rows — **different orderings on purpose**: one ranks a step's candidates against a shared parent, the other ranks models from different steps against each other. Re-reads outputs via `update_all_model_statuses` first.

### R/scm-selective-forward.R
_Selective forward (propagate only covariates from significant models) + redemption phase + checkpoint-resume._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `get_significant_models_from_step(search_state, step_number, p_value, rse_threshold=NULL, phase=c("forward", "backward"))` | ✓ | Models in a step clearing the per-covariate threshold + RSE, **for the named phase**. Pre-filters the DB to that step's `completed` rows with a non-`NA` `delta_ofv`, then returns the names the shared evaluator accepts. `phase` defaults to `"forward"`; a backward step judged with the forward rule reports the wrong models, because the two phases compare `delta_ofv` in opposite directions | step, p-value, `phase` | `character()` model names (`character(0)` on empty DB / missing cols / empty step) | `warning()` when a required column is absent | calls `.evaluate_model_criteria`; called-by `run_scm_selective_forward` (step winner + redemption) + `get_step_models` |
| `get_step_models(search_state, step_number, p_value=NULL, rse_threshold=NULL, phase=c("forward", "backward"))` | ✓ | **Supportive lookup:** reconstruct a step from the DB — base (most common parent), tested models, completed, significant, winner. Excludes the `phase=="base"` row (the base model can share a step number but is not a tested model). `phase` picks both the default p-value (`forward_p_value` vs `backward_p_value`) and which end of the ΔOFV ordering the winner sits at: forward `which.max`, backward `which.min` | step, `phase` | `list(exists, step_number, base_model, models, completed_models, significant_models, best_model)`; an all-empty list with `exists=FALSE` when the step has no non-base rows | none | calls `get_significant_models_from_step`; called-by `run_scm_selective_forward` (continuation branch, forward default) + `continue_search` (forward default) + `.scm_continue_backward` (`phase="backward"`) |
| `get_covariates_from_models(search_state, model_names)` | ✓ | Unique valid `^beta_` tags tested in given models | `model_names` | `character()` tags | verbose `cat` | called-by selective + resume |
| `run_scm_selective_forward(search_state, base_model_id=NULL, forward_p_value=NULL, rse_threshold=NULL, auto_submit=TRUE, auto_retry=TRUE, resume=FALSE)` | ✓ | **Selective forward:** first step = all covariates; later steps = only covariates from prior significant models; then redemption. `resume=TRUE` (from `continue_search`) makes the FIRST pass a continuation (selective from `get_step_models(last_step)`) instead of "test all". **Every continuation pass first blocks until the previous step is terminal:** a `repeat` loop takes the previous step's non-`base` rows, and while any is pending per `.is_pending_status()` it prints them, `Sys.sleep(60)`s and re-runs `update_all_model_statuses()`. Needed because `get_step_models()` reads only `status=="completed"` rows, so a model still running on the cluster would be invisible and the loop would create the next step (or a redemption model) on top of it. **Every re-base goes through `.best_acceptable_model()`** (4 sites: fallback base when a step has no winner, the redemption-base fallback, the per-redemption-step re-base, and the final post-redemption pick) — each used to rank on OFV alone, so a model the search itself had rejected could become the base everything after it builds on, including the model handed to backward elimination. Each site pairs with `.report_rse_rejections()`, and the final pick prints when it overrides the step winner. **No site treats "nothing qualifies" as terminal** — since `.best_acceptable_model()` never returns the base model, each names its own fallback: the fallback base pick uses `prev_step$base_model` (else `base_model_id`), the redemption-base fallback uses `base_model_id`, the per-redemption-step re-base keeps the `current_redemption_base` it already had, and the overall pick keeps the step winner. **All four pools exclude backward rows** via `!.scm_backward_rows(search_state)` — `all_previous_models` (fallback base pick), `all_main_models` (redemption-base fallback), `all_models_so_far` (per-redemption-step re-base), `all_completed_candidates` (overall pick) — because a backward row's `delta_ofv` carries the opposite sign convention and the forward test would read a costly removal as a large improvement. Enforced by the test *"every forward candidate pool in the file carries the phase filter"*, which greps the source for the four pool assignments and fails if any lacks the filter, so a fifth pool or a deleted guard breaks loudly. **The filter deliberately keeps the base model in `all_main_models`:** the base row's `phase` is `"base"`, which `.scm_phase_is_backward` does not match, so it stays in scope — load-bearing, because that site falls back to `base_model_id` when nothing qualifies and then looks it up in this very table (`final_best_idx <- which(all_main_models$model_name == final_best_model)`); filtering it out would leave `integer(0)` and make GUARD 2 skip redemption | base, p-value, rse, resume | `list(search_state, status, final_best_model, step_results, total_time_minutes)` | `save_search_state()` at 5 points, all named by `.scm_checkpoint_name()` → `<models_folder>/scm_rds/NN_forward_created.rds`, `NN_forward_done.rds`, `NN_redemption_created.rds`, `NN_redemption_done.rds`, `NN_forward_complete.rds`; **`Sys.sleep(60)` polling** in the continuation guard; `cat` | calls `get_remaining_covariates`, `.is_pending_status`, `update_all_model_statuses`, `get_step_models`, `get_significant_models_from_step`, `get_covariates_from_models`, `get_model_covariates_from_db`, `run_univariate_step`, `submit_and_wait_for_step`, `select_best_model`, `.best_acceptable_model`, `.report_rse_rejections`, `.resolve_rse_threshold`, `pvalue_to_threshold`, `.scm_checkpoint_name`, `save_search_state`; called-by orchestrator + `continue_search` |
**Notes:** `run_scm_selective_forward` = the biggest fn (main loop + Scenario A/B redemption). Fresh vs. continuation is an explicit `resume` flag (init `is_continuation <- isTRUE(resume)`, then TRUE after the first pass) — **not** inferred from the DB, because the base model can sit at any `step_number` (it's tagged `phase=="base"`, not fixed at step 0). The continuation branch reconstructs the previous step from the DB via `get_step_models`, so no in-memory `step_results` is needed to resume. `resume_selective_forward` was **removed** earlier (forward-only, broken); `continue_search` (below) replaces it for both phases.

### R/continue-search.R
_Resume an interrupted SCM run (forward or backward) from the last full step._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `continue_search(search_state=NULL, checkpoint=NULL, scm_type=NULL, full_scm=TRUE, forward_p_value=NULL, backward_p_value=NULL, rse_threshold=NULL, auto_submit=TRUE, auto_retry=TRUE)` | ✓ | Resume from last full step: re-read last-step files (`force=TRUE`), require step complete, detect phase from DB, find current best, re-enter the right method. Fwd→(bwd if `full_scm`). | in-memory state **or** checkpoint path; `scm_type` (req. for fwd resume); `full_scm` | invisibly `list(search_state, status, scm_type, resumed_phase, final_model, final_covariates)`; the early return is `list(search_state, status="incomplete_step", step, pending_models)` when any last-step model is still pending per `.is_pending_status()` | `cat`; `load_search_state`; `stop()` (no state/checkpoint, missing checkpoint file, empty DB, no completed step, unknown `scm_type` on a forward resume); delegates → method saves | calls `.scm_unregistered_models`, `reconstruct_step_from_disk`, `update_model_status_from_files`, `update_all_model_statuses`, `.is_pending_status`, `.resolve_rse_threshold`, `.scm_phase_is_backward`, `get_step_models`, `run_scm_selective_forward(resume=TRUE)`/`run_stepwise_covariate_modeling`/`run_backward_elimination`, `.scm_continue_backward` (→ `evaluate_removal_impacts`), `get_model_covariates_from_db` |
| `.scm_continue_backward(search_state, last_step, backward_p_value, rse_threshold, auto_submit, auto_retry)` | — | Re-evaluate the last backward step's removal models (rebuilt via `get_step_models(phase="backward")` — the **forward** default would judge those ΔOFVs with the wrong comparison) → rebuild the covariate-name→model-name map `evaluate_removal_impacts()` expects from each row's `covariate_tested` tag → winner → continue `run_backward_elimination` from the winner (last step is **not** re-created). Returns the reconstructed base unchanged when the step can't be rebuilt, has no completed models, or no removal qualifies | last_step, p-value, rse | `list(search_state, final_model)` | writes `step_description` for the winning row; `cat`; delegates to `run_backward_elimination`'s saves | calls `get_step_models`, `extract_covariate_name_from_tag`, `evaluate_removal_impacts`, `run_backward_elimination` |
| `.scm_phase_is_backward(phase)` | — | TRUE if phase matches `backward`/`removal` | phase | logical | none | called-by `continue_search`, `.scm_backward_rows` |
| `.scm_backward_rows(search_state)` | · | "Which rows carry a **backward-signed `delta_ofv`**" — the filter that keeps backward rows out of forward selection's candidate pools. The two phases store `delta_ofv` with opposite sign conventions, so a removal that badly worsened the fit reads to the forward test as a large improvement; a search that *begins* with backward elimination has exactly such rows. **A row counts as backward if EITHER its `phase` says so OR its `action` does** (`by_phase | .is_removal_model(search_state, db$model_name)`) — neither alone suffices. `phase` is overwritten by two paths (`create_retry_model()` writes `"retry"`; `discover_existing_models()` rewrites rebuilt rows to `"manual"`), so phase-only missed a retry of a failed removal — which is not hypothetical, since a retry inherits the original's `step_number` and its `delta_ofv` starts `NA` but is overwritten with the **backward** sign the moment the model completes. `action` closes it because `.is_removal_model()` resolves a retry to the model it retries — **the same resolution `update_model_status_from_files()` uses to choose the sign in the first place**, which is what makes the match exactly as retry-aware as the code that created the sign, rather than merely broader. **The base model is matched by neither test** (`phase="base"`, `action="base_model"`) so it stays in scope, which the redemption-base fallback depends on | `search_state` (not a bare db — `.is_removal_model` needs the state) | logical vector, one element per database row; **`logical(0)`** for NULL / non-`data.frame` DB. Degrades safely: no `phase` column ⇒ phase term all-`FALSE`; no `action` column ⇒ `.is_removal_model` all-`FALSE`; the union is whichever evidence survives | none | calls `.scm_phase_is_backward`, `.is_removal_model`; called-by `run_scm_selective_forward` (**all four** of its candidate pools — see that row) |
**Notes:** No new state field — `scm_type`/`full_scm` are **args** (DB doesn't record the forward method; thresholds come from `search_config`). Method/phase re-entry reuses the existing methods: **standard fwd** and **backward** are already DB-driven (pass the current best as the start); **selective fwd** gets `resume=TRUE`. Forward is skipped (jump to backward) when the last step yielded no significant model. Requires the last step to be fully terminal — the test is `.is_pending_status()` (`created`/`submitted`/`in_progress`/`incomplete`/`unknown`), not an inline vector, so the `"incomplete"` spelling that `get_model_status_from_files`/discovery write is caught too; else it returns `incomplete_step` with the offending model names. Winner + significance for the last forward step come from `get_step_models` (same evaluator as the live selective loop; RSE-aware) — the old `.scm_current_best_forward` heuristic was removed.

**Why the backward-row filter tests two fields (closed 2026-08-07):** `.scm_backward_rows` used to key on `phase` alone, and a retry launders it — `create_retry_model` writes `phase="retry"` while keeping the original's `step_number`, and the delta is later filled with the **backward** sign because `.is_removal_model()` resolves the retry back to the removal it retries. The row was backward in substance, `"retry"` in `phase`, and carried a non-`NA` backward-signed delta straight into all four forward pools. The fix ORs in `.is_removal_model()`, the *same predicate* the sign-writer consults — so the filter and the sign cannot disagree by construction.

**Sign/direction consistency — the current, settled picture (2026-08-07).** Three mechanisms interlock, and all three now read the same predicate:
1. `.signed_delta_ofv()` is the sole writer of the ΔOFV sign; `.is_removal_model()` is the sole direction lookup; `.scm_backward_rows()` is the sole pool filter, ORing phase with that same lookup.
2. `.is_removal_model()` itself now **falls back to `phase` for rows whose `action` cannot answer**, so the older failure — a database with no `action` column giving a removal the forward sign while `.scm_backward_rows` read it as backward from its phase — is gone. The two readers can no longer disagree on such a row. `action` remains authoritative where present; a phase that contradicts it is left as a fault to report.
3. The residual risk is no longer a *silent* disagreement but a **mis-assembled candidate list**, and both direction-stating callers (`select_best_model`, `evaluate_removal_impacts`) `warning()` on being handed a model of the wrong kind. That is the one seam left, and it is loud.

### R/resume-reconstruct.R
_Re-register an interrupted step's models from disk when they're absent from every saved checkpoint (human-gated resume fallback for the gap before the first per-step checkpoint)._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `reconstruct_step_from_disk(search_state=NULL, step_number=NULL, base_model=NULL, direction=NULL, checkpoint=NULL, models_folder=NULL)` | ✓ | Register on-disk `run<N>` models missing from the DB (excl. `run<N>001` retries). **Auto-detects** base (candidates' common `based_on`), direction (they *add*→forward / *remove*→backward a covariate), step (`max+1`) — each overridable. Inserts schema-safe rows (`phase`/`action`/beta-tag as a live run) with `created_by_search=TRUE` set explicitly — an earlier session of *this* search created them, so the search may still resubmit them, and the row is copied from the base model, which is `FALSE`. Resets counter, reads results from files. **Errors** (not guesses) if candidates disagree (>1 parent, or mixed add/remove). | in-memory state **or** `checkpoint` path; optional `step_number`/`base_model`/`direction` overrides | updated `search_state`; prints what it registered + any non-`completed`/skipped | `cat`; reads `.yaml`+output files | calls `.scm_unregistered_models`, `.scm_model_parent`, `load_search_state`, `yaml::read_yaml`, `update_model_status_from_files`, `update_model_counter`; called-by `continue_search` (auto) |
| `.scm_unregistered_models(search_state, models_folder=NULL)` | · | `run<N>` model dirs on disk not in the DB (excl. `run<N>001` retries) | state | `character()` model names | none | called-by `reconstruct_step_from_disk`, `continue_search` |
| `.scm_model_parent(models_folder, model)` | · | Recorded parent (bbr `based_on`) of a model, from its `.yaml` | folder, model | `character(1)` or `NA` | reads `.yaml` | called-by `reconstruct_step_from_disk` |
**Notes:** The **normal** path is the save-at-creation checkpoints (`NN_phase_created.rds`) written before each step submits, so a mid-step crash already leaves the step in a checkpoint; this is the fallback when even that is missing, and `continue_search` calls it automatically when it finds on-disk models absent from the DB. Base/direction/step are inferred from the files (bbr `based_on`, tag-diff, `max+1`) but overridable. What still *can't* be inferred is a Metworx-dropped run (files unchanged) — you rerun that model, then resume.

### R/scm-backward.R
_Backward elimination + removal-impact evaluator + covariate name/tag/FIX helpers._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `run_backward_elimination(search_state, starting_model, backward_p_value=NULL, auto_submit=TRUE, auto_retry=TRUE, rse_threshold=NULL)` | ✓ | Iteratively remove least-impactful covariate (smallest ΔOFV increase below threshold) until none qualify. FIX-flagged covariates are excluded from the removable set; an all-FIX model ends the loop. Its console criteria lines no longer say "NA RSE accepted" — that stopped being true under the default `require_cov_step=TRUE` | starting model, p-value, rse | `list(search_state, status, starting_model, final_model, removed_covariates, steps_completed, step_results, total_time_minutes, starting_ofv, final_ofv, final_covariates)` | `save_search_state()` at 3 points, named by `.scm_checkpoint_name()` → `<models_folder>/scm_rds/NN_backward_created.rds` (**before** submitting, so a mid-step crash still leaves the step on disk), `NN_backward_done.rds`, `NN_backward_complete.rds`; `stop()` if the starting model is absent from the DB or has no OFV; `cat` | calls `.resolve_rse_threshold`, `get_model_covariates_from_db`, `get_fixed_covariates`, `get_covariate_tag_from_name`, `remove_covariate_from_model`, `submit_and_wait_for_step`, `evaluate_removal_impacts`, `pvalue_to_threshold`, `.scm_checkpoint_name`, `save_search_state`; called-by orchestrator (`full_scm_search.R` ×2), `continue_search`, `.scm_continue_backward` |
| `evaluate_removal_impacts(search_state, base_model, removal_models, completed_models, backward_p_value, rse_threshold=NULL)` | ✓ | ΔOFV per removal candidate, then pick the smallest one meeting the criteria. **Two passes:** (1) write each removal's ΔOFV onto its own DB row in backward's sign convention (`model − base`, positive = the removal made the fit worse), skipping candidates that did not complete or have no OFV; (2) hand the eligible names to `.evaluate_model_criteria(phase="backward")` — the same evaluator forward uses — so the number judged is the number the DB carries. **The one documented exception to the shared rule:** it does *not* use the evaluator's `meets_threshold`, recomputing it as `meets_ofv & meets_rse` and **dropping the `status == "completed"` term** on purpose. Eligibility belongs to the caller's `completed_models` argument, applied in pass 1 — and `submit_and_wait_for_step()` returns `completed_models = model_names` **wholesale** when `auto_submit = FALSE` (status `"submission_skipped"`), so re-gating on DB status here would overrule the caller and stall backward elimination in the manual workflow. Winner = smallest `delta_ofv` among `meets_threshold` rows | removal_models (cov→model), completed_models, p-value, rse | `list(search_state, removal_impacts, covariate_to_remove, new_base_model, delta_ofv, ofv_threshold, covariate_df, removable_count)`; the last four are `NULL`/`NA` when nothing qualifies | writes `delta_ofv` back to `search_state$search_database` via `.signed_delta_ofv(direction="backward")` — **explicit, not derived**, because this function also fixes the evaluator's `phase`, and on a database with no `action` column a derived sign would read every row as an addition and invert the verdict; **`warning()`** when a supplied model's own action is not a removal (a mis-built `removal_models`); `stop()` if the base model row/OFV is missing; `cat` (per-candidate verdict line) | calls `.resolve_rse_threshold`, `.signed_delta_ofv`, `.is_removal_model`, `.evaluate_model_criteria`, `pvalue_to_threshold` (the "no removals qualify" message only); called-by `run_backward_elimination`, `.scm_continue_backward` |
| `get_fixed_covariates(search_state, covariate_names)` | ✓ | Covariates flagged FIX (via `covariate_search$FIX_STATUS`) | names | `character()` fixed | — | called-by `run_backward_elimination` |
| `get_covariate_tag_from_name(search_state, covariate_name)` | ✓ | Reverse-map name→tag w/ pattern fallback | name | tag or `NULL` | — | called-by `run_backward_elimination` |

**Notes:** Removal candidates built via `remove_covariate_from_model(save_as_new_model=TRUE)` (wrapped `suppressMessages`), then rows stamped `phase="backward_elimination"`, **`action="remove_covariate"`** (the patch masking the `remove_single_covariate` root bug). `get_fixed_covariates` only checks `FIX_STATUS`; THETA-FIX file parse is a TODO (unimplemented). `evaluate_removal_impacts`'s `removal_impacts` frame is now built in one shot from the evaluator's columns (`covariate, model_name, ofv, delta_ofv, rse_max, ofv_threshold, covariate_df, meets_ofv, meets_rse, meets_threshold`) — the old grow-by-`rbind` with a mismatched 8-col seed is gone.

---

## Utilities

### R/utilities.R
_YAML/model-name helpers + SCM statistics (p-value→ΔOFV thresholds, covariate df, tag parsing, working-file cleanup)._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `clean_dir(models_folder="models")` | ✓ | Delete all `WK_*` NONMEM working files | folder | `integer(1)` count | **file deletion** (`file.remove`); `cat` | — |
| `extract_covariate_name_from_tag(tag)` | ✓ | Covariate name from `beta_COV_PARAM` | tag | `character(1)` or `NA` | `warning` | called by evaluation/selection/validation |
| `.scm_checkpoint_name(step, phase, event)` | · | `scm_rds/` checkpoint filename `NN_phase_event.rds` — zero-padded step first so the folder sorts chronologically and highest `NN` is the latest | step, phase, event | `character(1)` | none | called by every `save_search_state` site (fwd/bwd/selective/redemption/monitor/full-SCM) |
| `.resolve_rse_threshold(search_state, rse_threshold=NULL)` | · | **The single place the RSE limit is resolved**: explicit argument → `search_config$max_rse_threshold` → `.DEFAULT_RSE_THRESHOLD`, where each candidate must be *usable* (`is.numeric` && `length==1` && `!is.na`) or it is skipped for the next one — so `NA` is rejected as firmly as `NULL`, and the function **falls back rather than erroring**. Every comparison against an `NA` threshold is `NA`, and an `NA` verdict indexes out as an `NA` model name instead of dropping, so an unusable value must never leave here. Everything that compares an RSE against a limit — the acceptance tests, the results table, the text report, the monitoring warning — reads it through here, so a search cannot apply one limit in one place and a different one somewhere else | state, optional override | `numeric(1)` percentage | none | called-by `.evaluate_model_criteria`, `select_best_model`, `run_backward_elimination`, `evaluate_removal_impacts`, `run_scm_selective_forward`, `create_scm_results_table`, `generate_scm_report`, `continue_search`, `run_automated_scm_testing`, `update_model_status_from_files` (the high-RSE warning) |
| `pvalue_to_threshold(p_value, df=1)` | ✓ | p-value → χ² ΔOFV threshold via `qchisq`; **`df=0` returns 0** (the fully-FIX case) | p, df | `numeric(1)` | `stop()` | called widely |
| `calculate_covariate_df(covariate_name, covariate_search)` | ✓ | df = number of **ESTIMATED (non-FIX)** parameters the covariate adds: per-level categorical → n_levels−1; single-factor → the formula's theta count (1 built-in, N for an N-parameter expression) minus those marked `FIX` in `INIT`. A fully-FIX covariate returns **0** → threshold 0 → kept on any ΔOFV > 0 | name, table | `integer(1)` | `warning` | reads the registry + `INIT`; called widely |
| `.load_if_path(x, what="input")` | · | Let any data/table argument be the object OR a path: length-1 character → load by extension (`.csv` via `readr::read_csv`, `.rds` via `readRDS`), anything else returned unchanged. Models are deliberately NOT handled (they stay name-based against `models_folder`) | object or path | the in-memory object | `stop()` if the path doesn't exist / extension unsupported | called by the forest + scenario entry points |
| `.is_terminal_status(status)` | · | **The single definition of "this model has a final answer"**: `completed`, `failed`, `estimation_error`. Nothing else settles a model | character vector of DB statuses | logical vector; **`NA` ⇒ `FALSE`** | none | called-by `.is_pending_status`; the terminal list is defined here and nowhere else |
| `.is_pending_status(status)` | · | **The single definition of "this model has not finished yet"** — use it for any "is the step still running?" guard instead of spelling out a status vector. **Defined by inversion**: `!.is_terminal_status()`, so a status nobody anticipated blocks rather than being read as finished. That is the point — the unfinished states are spelled inconsistently (`update_model_status_from_files` writes `"in_progress"` where `get_model_status_from_files`, the reader `discover_existing_models` seeds a row with, writes `"incomplete"`; `created`/`submitted` survive from before either reader ran; `"unknown"` is a never-persisted initialiser), none can be renamed because checkpoints on disk hold them all, and two hand-written lists that both omitted `"incomplete"` are what let a step be declared finished with a model still estimating | character vector of DB statuses | logical vector; **`NA` ⇒ `TRUE`** (not known to have finished) | none | called-by `continue_search` (last-step completeness gate → `status="incomplete_step"`), `run_scm_selective_forward` (the wait-before-advancing loop), `update_all_model_statuses` (which rows to re-read). **Both guards additionally require `.is_search_model()`**: `update_all_model_statuses` never re-reads a model the search did not create, so such a row can never leave a pending status and waiting on it would never end |
| `.is_search_model(search_state, model_name)` | · | Provenance test behind the rule "the search only runs models it created": looks each name up in the DB and reads `created_by_search`. Run number is deliberately **not** the test — the model a phase builds on moves as the search runs, so "newer than the base" is not stable. A name with no DB row ⇒ `FALSE`; the column missing entirely ⇒ `TRUE` for every name (pre-guard behaviour); an `NA` in the column ⇒ `FALSE` | state, character vector of model names | logical vector, one element per `model_name` (`logical(0)` for empty input) | none | called-by `submit_and_wait_for_step` (submission guard + retry submission), `update_all_model_statuses` |
| `.ensure_provenance_column(search_state)` | · | Migrate a checkpoint written before `created_by_search` existed: adds the column with every existing row marked `TRUE` (how the search treated them pre-guard). Needed because the first appended row that *has* the column makes `dplyr::bind_rows` back-fill `NA` into all older rows (reads as "not search-created" → freezes the prior history out of the search) and makes `rbind` fail on the width mismatch | `search_state` | `search_state` with `created_by_search` on its DB; returned unchanged if there is no data.frame DB or the column is already there | `cat` (only when the DB has ≥1 row) | called-by `load_search_state`, `create_retry_model` |
| `.is_removal_model(search_state, model_name)` | · | Direction test — is this model a covariate **removal** rather than an addition? Looks each name up in the DB and reads `action`, then resolves retry rows: `create_retry_model` writes `action="retry"`/`phase="retry"`, so a retry carries no direction of its own and only the `original_model` it points at does. TRUE when the **resolved** action matches `/remove/i`, which catches both written spellings (`remove_covariate` and `remove_single_covariate` from `model-discovery.R`); `add_covariate`/`add_single_covariate`, `manual_modification` and `base_model` are FALSE. **Falls back to `phase` only for rows the action could not answer** (`NA` action, dangling/unresolved retry, or no `action` column at all): such a row is treated as a removal when `.scm_phase_is_backward(phase)`. **`action` stays authoritative** — a row that records one keeps that answer *even when its phase contradicts it*, because a contradiction is a data fault to report, not to reinterpret, and the two sign-writing callers warn about exactly that. The fallback closes the case where a database with no `action` column gave a removal step the forward sign here while `.scm_backward_rows()` still read it as backward from its phase | state, character vector of model names | logical vector, one element per `model_name` (`logical(0)` for empty input). FALSE for a name with no DB row, and when **neither** action nor phase names a direction | none | called-by `.signed_delta_ofv` (ΔOFV sign), `.scm_backward_rows` (forward-pool filter), `evaluate_removal_impacts` (mis-built-pool warning), `submit_and_wait_for_step` (suppress retry for failed removals) |
| `.signed_delta_ofv(search_state, model_name, model_ofv, reference_ofv, direction=NULL)` | · | **The one place the ΔOFV sign convention is chosen.** An ADDITION stores `reference − model` (positive = the covariate improved the fit); a REMOVAL stores `model − reference` (positive = dropping it made the fit worse). With `direction=NULL` it is read from the row via `.is_removal_model()` — the same predicate the acceptance tests and `.scm_backward_rows` use, which is what stops a row's stored delta and the rule that later reads it from disagreeing. See the `direction` rule below the table before choosing | state, model name, its OFV, the reference OFV (parent for forward, step base for backward), optional `direction` | `numeric(1)`; **`NA_real_`** if either OFV is missing or zero-length | none | calls `.is_removal_model`; called-by `update_model_status_from_files` (`NULL` — writes only), `select_best_model` (`"forward"`) and `evaluate_removal_impacts` (`"backward"`), both of which also fix the test's direction and warn on the other kind |
| `decode_dataset(data, yaml_file, column_names)` | ✓ | Replace numeric category codes with their decoded labels for the named columns, using a spec/lookup's `values`→`decode` mapping | data, lookup/spec, columns | `data` with decoded columns | — | used by the plotting/EDA side |

**Notes:** `analyze_*` contain duplicate `run1`→BASE / `\d{3}$`→RETRY regex; both are exported-but-unused. `tags_list` arg unused. No `globalVariables`/`@importFrom` here.

**`.DEFAULT_RSE_THRESHOLD` (= 50)** is a package-level *constant*, not a function, so it has no row above. It is the one place the default RSE limit lives: `initialize_search_config()` writes it into a new search's config and `.resolve_rse_threshold()` falls back to it, so the value a search is *created* with and the value an older state is *read* with cannot drift apart.

**When to pass `.signed_delta_ofv(direction=)` and when not to — the rule, not just the signature.** `direction` looks like a wart; it is not. `action` is **not** in `update_model_status_from_files()`'s self-heal `required_cols`, so a database can legitimately lack the column. The rule turns on **what the caller ALSO does**, not on whether its rows are of one kind:
- **Writes the sign AND fixes the acceptance test's direction ⇒ state `direction` to match, and warn on the other kind.** Both `evaluate_removal_impacts` (`phase="backward"` ⇒ `direction="backward"`) and `select_best_model` (`phase="forward"` is a literal ⇒ `direction="forward"`) are in this case. Deriving instead lets the sign and the test disagree whenever the lookup cannot answer: a removal row stored as `model − parent` (say `+50`) and then tested with `delta_ofv > threshold` reads as a 50-point *improvement* and gets **selected**.
- **Writes only, never tests ⇒ leave `direction` NULL** and let each row's own action decide. `update_model_status_from_files` is the sole such caller.

Get this backwards and the failure is silent — a stored delta whose sign disagrees with the rule that reads it. Once sign and test agree, the only way left to be wrong is a mis-assembled candidate list, which is why **both** direction-stating callers `warning()` when handed a model of the opposite kind.

---

## Reporting & tables

### R/reporting.R
_Renders the search database into a formatted plain-text report file._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `generate_scm_report(search_state, output_file="scm_report.txt", print_console=TRUE)` | ✓ | Step-by-step forward/backward/redemption text report. **No longer a dissenting implementation:** every per-model ✅/❌ comes from `.evaluate_model_criteria()` (per-covariate df, the phase's own p-value and comparison direction, and the RSE limit), step type comes from `.scm_backward_rows()` keyed by model name so it survives the sort, and each step's winner is the best among models meeting **both** criteria — `which.min(delta_ofv)` backward, `which.max` forward. **The final model is tracked through the loop**: seeded from the first step's parent, advanced only by an accepted winner, held when a step produces none, falling back to `search_state$base_model`. The old `which.max(step_number)` named whatever ran last, accepted or not, which is how a rejected model could be reported as the result. Non-acceptance is explained in the caller's vocabulary (`reason_for`), and a step where everything was blocked on RSE says so with a count | `search_state`, output path | `invisible(NULL)` | **file write** (`writeLines`); `cat`; `stop()` if DB empty | calls `.resolve_rse_threshold`, `.scm_backward_rows`, `.evaluate_model_criteria`, `stats::setNames`; nested `reason_for`; redefines local `%||%` |

**Notes:** Covered by `tests/testthat/test-scm-report.R` (added 2026-08-07) — the rewrite initially shipped with no tests at all. Local `%||%` shadow (redundant — the package defines one). Step type is `.scm_backward_rows()`; **redemption is still a `phase` grep** (`redemption|final_test|retry`) and takes priority over the backward label. Hardcoded `scm_report.txt` (cwd). Counts in the final summary use `na.rm = TRUE`, so an `NA` status no longer propagates. The long-standing `base_ofv` hazard is **fixed (2026-08-07)**: it is reset to `NA_real_` at the top of each step and the backward header line is guarded, so a step whose rows carry no `parent_model` prints "Base model OFV not available" instead of erroring on the first step or silently reporting the *previous* step's base OFV as its own.

### R/scm-results.R
_Builds + pretty-prints the enhanced SCM results table with per-model RSE/OFV selection verdicts._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `create_scm_results_table(search_state)` | ✓ | Per-model results w/ df-aware forward/backward verdicts (`BEST`/`YES`/`NO`/`KEPT`/`REMOVED`) + comments. **The verdict now comes from `.evaluate_model_criteria()` — the same evaluator the search itself used — so this table reports the decision that was taken rather than a second opinion on it**; only the label vocabulary is local. Phase per row is inferred from the step's `phase` string (`grepl("backward|remove")`), and `BEST` is decided by re-evaluating the whole step and comparing against `max(delta_ofv)` among its accepted models | `search_state` or wrapper list | `data.frame(Phase_Step, Model, Parent_Model, Description, Status, Covariates_in_Model, OFV, Delta_OFV, RSE_Max, Selected, Comment)` | `cat`; reads per-model `.yaml` | calls `.resolve_rse_threshold`, `.evaluate_model_criteria` (twice per row: the row itself, then its step for the `BEST` test), `pvalue_to_threshold`, `yaml::read_yaml`; called-by `print_scm_results_table` |
| `print_scm_results_table(search_state, show_rse=TRUE, truncate_covariates=35)` | ✓ | Console-format table + summary | df or state | `invisible(results)` | `cat` | calls `create_scm_results_table` |

**Notes:** Nested helpers `get_model_covariates_display` (YAML tags→`COV_on_PARAM`) and `get_selection_info` (label engine — it no longer re-derives the df threshold or the OFV/RSE comparison; it calls `.evaluate_model_criteria()` and translates `meets_ofv`/`meets_rse` into `BEST`/`YES`/`NO`/`KEPT`/`REMOVED` plus a comment string, with the backward branch reading `meets_ofv` as "the removal costs little enough to accept"). `Phase_Step` derived from **`phase`+`step_number`, not `action`** (so it's not a pure display-registry lookup). Row-appends via `rbind` in a loop (quadratic). Unwraps `$search_state` at L13-14. Sorts by `gsub("^run","",Model)`, with **both `as.numeric()` coercions wrapped in `suppressWarnings()`** — the `NA`s are intended (a model name that is not `run<N>`, and the "Base" row, which the next line maps to 0), so the coercion warning is noise; unsuppressed it becomes an *error* for anyone running under `options(warn = 2)`.

### R/model-output-tables.R
_NONMEM control/`.cov`/`.cor` parsers + the flextable parameter-report pipeline (`get_param2` → `model_report`)._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `theme_pps_table(x)` | ✓ | Apply PPS/Times-New-Roman theme to a flextable | flextable | themed flextable | `stop()` if not flextable; `flextable::`/`officer::` | called-by `model_report` |
| `extract_params(lines, block_tag, remove_prefix=FALSE)` | · | Parse a `$THETA`/`$OMEGA`/`$SIGMA` block → name+transform (split each line on `;`) | ctl lines, tag | `tibble(param, trans)` | — | calls `purrr::map_dfr`/`tidyr::separate`; called-by `extract_model_params` |
| `extract_model_params(model_name, models_folder="models")` | · | All THETA/OMEGA/SIGMA params for a model | name, folder | `list(THETAS, OMEGAS, SIGMA)` of tibbles | `stop()` if no ctl | calls `find_model_file`, `extract_params`, `readLines`; called-by `get_param2`, `sample_individual_thetas` |
| `calculate_condition_number(model_number, models_folder="models", tolerance=1e-10)` | · | Condition number (max/min eigenvalue) from `.cor` (fallback `.cov`) | model, folder | `numeric(1)`/`Inf`/`NA` | reads `.cor`/`.cov` | calls `stringr::`, `eigen`; called-by `get_param2` |
| `decode_cov_level(cov_name, level, lookup)` | · | Categorical level→decoded label via lookup | cov, level, lookup | `character(1)` or `NA` | — | called-by `get_param2`; **unit-tested** |
| `get_param2(model_number, count_model, shrinkage="etasd", models_folder="models", spec_pk=NULL, lookup=NULL)` | · | One model's formatted parameter/estimate/RSE/shrinkage table (+OFV, cond number, beta_-tag labels) | model, count, shrinkage | `data.frame` (single- or multi-model shape) | `bbr::read_model/model_summary/param_estimates`; `cat` | calls `calculate_condition_number`, `extract_model_params`, `decode_cov_level`; called-by `model_report`, validation (`get_model_max_rse`, `update_model_status_from_files`) |
| `model_report(model_names, shrinkage="etasd", models_folder="models", spec_pk=NULL, lookup=NULL)` | ✓ | Per-model `get_param2`, join, group, render flextable | names | `flextable` | `cat`; `stop()` if all fail | calls `get_param2`, `theme_pps_table`, `flextable::`/`purrr::reduce` |

**Notes:** `extract_*`, `calculate_condition_number`, `decode_cov_level`, `get_param2` are documented-internal (no `@export`). `theme_pps_table` has both `@keywords internal` **and** `@export` → exported. `get_param2` beta_-tag label parse **duplicated** (label col + short col). `shrinkage` `case_when` selects a length-2 vector (fragile recycling).

### R/plot_nonmem_iterations.R
_Reads NONMEM `.ext` iteration history and plots parameter/OBJ trajectories. (Header comment mislabels the file `ext-file-utilities.R`.)_

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `read_ext_iterations(ext_file)` | ✓ | Parse `.ext` → tidy iteration table (split TABLE blocks, standardize OBJ, classify `TYPE` ITER/BURN/FINAL/SE/EIGEN/CONDNUM, flag EVALUATION) | `.ext` path | `data.frame(ITERATION, params…, OBJ, EST.NO, EST.NAME, TYPE, EVALUATION)` or empty | `stop()` if multiple OBJ cols | calls `utils::read.table`/`scan`; called-by `plot_nonmem_iterations`, `sample_individual_thetas` |
| `.listing_column_meta(ext_cols, model_name, models_dir)` | · | **Decides what each `.ext` column is called and which are log-scale**, from the control stream's own `$THETA`/`$OMEGA`/`$SIGMA` comments — it does not touch data. Names come from the comment's 2nd `;` field, the log flag from its 4th. **Only THETAs are ever rescaled**: a `;LOG` theta is stored as `log(value)`; OMEGA/SIGMA are variances and are reported as NONMEM wrote them (no CV% conversion — that is a `model_report()` rendering choice, not a trajectory one) | `ext_cols` = **full** `.ext` column names (pre-drop, so per-record counts match the control stream), model name, models dir | `list(labels=<named chr, one per ext col>, log_cols=<chr, columns to exp()>)` | `warning()` only, never `stop()`: control stream unreadable → everything falls back to `.ext` names/values; **per-record count mismatch** → that record alone is skipped (named in the message) while the others are still applied | calls `extract_model_params` (via `tryCatch`+`suppressWarnings`) → reads control stream from disk; called-by `plot_nonmem_iterations` |
| `plot_nonmem_iterations(model_name, models_dir="models", transform=TRUE, skip_iterations=0, obj_var="OBJ", max_iterations=100)` | ✓ | Faceted ggplot of the objective column + non-fixed parameter trajectories for the last non-Evaluation `$EST` step (the only step, if there is just one); BURN iterations renumbered to precede ITER | name, dir; `transform=TRUE` **names panels from the control stream and exponentiates `;LOG` THETAs** (see `.listing_column_meta`), `FALSE` plots the `.ext` verbatim — raw values *and* raw column names; `obj_var` takes the **`.ext` column name** (`THETA1`, not `CL` — it selects before annotations are read); `max_iterations` is an ITERATION **ceiling**, not a count; `skip_iterations` drops that many from the start of each `$EST` | `ggplot` | `stop()` on non-scalar `model_name`, missing `.ext`, empty/iteration-free data, non-logical `transform`, or `obj_var` not a column of the `.ext` (message lists available columns); inherits `.listing_column_meta`'s warnings | calls `read_ext_iterations`, `.listing_column_meta` (passing `ext_cols`, captured from the full `.ext` **before** constant columns are dropped, so per-record counts match the control stream regardless of `obj_var` and FIX-parameter removal), `tidyr::pivot_longer`, `dplyr::filter`, `ggplot2::` |

**Notes:** Own `utils::globalVariables(c("ITERATION","TYPE","value","variable"))`. Uses `@import ggplot2` (full). `read_ext_iterations`' data.frame is consumed **positionally** by `sample_individual_thetas` (col 2 → OBJ), so its column layout/`TYPE` labels are a hard contract. `plot_nonmem_iterations` also depends on that layout, but by name: it takes the parameter columns as everything between col 2 and the `OBJ` column, so `obj_var` other than `"OBJ"` must still sit inside that span. The CV%/`;LOG` rules are a **fourth** consumer of the `$THETA` trans annotation (see the ×3 parser note above) — it reuses `extract_model_params` rather than reparsing, so keep it that way.

---

## Exposure-forest simulation pipeline (7 files)

_Data flow: `sample_individual_thetas` → `create_covariate_table` → `apply_covariate_model` (via `build_scenario_parameters`) → `simulate_scenario_profiles` → `plot_exposure_forest`. `create_covariate_boxplots` is a separate branch._

### R/forest-plots.R — theta uncertainty sampling
| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `sample_individual_thetas(model, models_folder="models", Nsamples=1e+05, seed=1234)` | ✓ | Draw `Nsamples` THETA vectors from estimation uncertainty; returned **raw on the estimation scale** (log params stay log — no back-transform here; it lives in `apply_covariate_model` to avoid a double `exp`) | model, N, seed | `data.frame(ID, THETA1..n)` + attr `sampling_method` | `mvtnorm::rmvnorm`; `set.seed`; `stop`/`warning` | calls `.find_model_component`, `read_ext_iterations`, `.read_theta_cov`, `.make_psd` |
| `.find_model_component(model, models_folder, exts)` | · | Locate model file (flat/subdir) | exts | path or `NA` | — | calls `find_model_file` |
| `.read_theta_cov(model, models_folder, theta_names)` | · | `n×n` THETA covariance from `.cov`(pref)/`.cor`, zeros for fixed | theta_names | matrix or `NULL` | — | calls `.find_model_component`, `.read_nonmem_matrix` |
| `.read_nonmem_matrix(mfile)` | · | Parse labelled square `.cov`/`.cor` (last TABLE) | path | matrix or `NULL` | `readLines` | called-by `.read_theta_cov` |
| `.make_psd(s, tol=1e-08)` | · | Symmetrise + nearest-PSD projection | matrix | PSD matrix | — | called-by `sample_individual_thetas` |

**Notes:** Dup targets: `.read_nonmem_matrix` vs `calculate_condition_number`; uses `extract_model_params` for `$THETA` trans. Exported and in NAMESPACE.

### R/forest-plots.R — covariate scenario table
| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `create_covariate_table(model_name, covariate_search, data, percentiles=c(0.05,0.95), models_folder="models", id_col="ID", lookup=NULL, spec=NULL, wrap_width=30)` | ✓ | Null-patient + single-covariate-variation scenario table | search table, data, percentiles | `data.frame`: col1 `Scenario` + one col per covariate; row1 all-reference | `readLines`; `stats::quantile`; `warning`/`stop` | nested helpers; called-by `build_scenario_parameters` |
| `join_wrap/cov_label/cov_unit/decode_level/ordinal/cont_scenario` | · nested | Label/format helpers | — | char scalars | — | nested |
| `.spec_to_lookup(spec)` | · | Turn a yspec object (or a path to one) into the package's `lookup` shape (`values`/`decode` per covariate) so spec and lookup are interchangeable wherever categoricals are decoded | yspec object / path | `list` lookup | `yspec::ys_load` | called by the table + plot entry points |

**Notes:** Independently re-parses `$THETA` to find `beta_<COV>_<PARAM>` names + rebuilds `cov_to_test`; nested `decode_level` duplicates `decode_cov_level`. No undeclared deps. `man/create_covariate_table.Rd` exists.

### R/forest-plots.R — scenario summary tables
_Absolute + relative (ratio-to-reference) summary tables, written as RDS next to the plots by both plot functions. No `*_info` argument ⇒ no table._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `.scenario_summary_table(long, quantities, q_info, scenario_tbl, reference, percentiles=c(0.05,0.95), lookup=NULL, relative=FALSE, digits=2)` | · | Rows = scenarios, leading covariate columns from the scenario table (decoded, units in the header), one column per quantity. Absolute cell = `median\n[lo,hi]\ngeomean\n(geoCV%)`; relative cell = `ratio [lo,hi]` (median/lo/hi ÷ the reference scenario's median, ref row = `1 [lo,hi]`). geoCV% = `sqrt(exp(var(log x))−1)·100` | long data, quantity names, label/unit info, scenario table | `data.frame` of formatted strings | — | called-by `plot_exposure_forest`, `plot_parameter_forests` |
| `.resolve_quantity_info(x)` | · | Normalise `metric_info`/`param_info` given as a named `list(label, unit)` **or** a yspec object / spec path / spec-shaped list (maps `short`→header label + `unit`). Recognises a real yspec object rather than falling back to the long label | list or spec | named list of `list(label, unit)` | `yspec::ys_load` | called-by both plot functions |
| `.require_quantity_labels(q_info, quantities, arg="metric_info")` | · | **Stops** if any tabulated quantity has no label/unit — the user must label every quantity that reaches a table | resolved info, quantity names | `invisible` | `stop()` | called-by both plot functions |

**Notes:** Metric/parameter **column names must match the spec keys** (user's responsibility). Exposure tables span every metric column of `data` and are re-written per metric call; parameter tables cover every plotted structural parameter and are a **named list keyed by parameter** (one table per parameter, only the covariate columns that vary), saved as `<model>-{exposure,parameter}-table-{absolute,relative}.rds`.

### R/apply-covariate-model.R
| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `apply_covariate_model(model_name, covariate_search, individual_thetas, covariates, models_folder="models")` | ✓(in NAMESPACE) | **Evaluate the model's `$PK` equations** (ETA=0, THETA from draws, covariates from scenario/`REFERENCE`) → natural-scale structural params for one scenario. Handles log/normal/categorical/multi-theta uniformly (equation's `EXP` does the transform once) — no transform detection, no read-side registry, no drift. Small `$PK` interpreter with `IF/ELSEIF/ENDIF` + operator translation | search table (+REFERENCE), raw estimation-scale thetas, one scenario | `data.frame(ID, structural THETA cols)` natural scale | `readLines`; `warning`/`stop` | self-contained; called-by `build_scenario_parameters` |
| `get_block/first_theta_index` | · nested | Control-stream parsing: `get_block(tag)` returns the lines of every `$<tag>` record (header + body up to the next `$`), `first_theta_index(txt)` returns the **first** `THETA(n)` index in an expression — that is how a `$PK` definition line is pinned to the structural THETA position it defines | `lines` (closed over), block tag / expression text | `character` block lines / `integer` (`NA_integer_` when none) | — | nested in `apply_covariate_model`; `forest-plots.R`'s `.structural_param_names` carries a duplicate pair |
| `nm_to_r/eval_r/is_active/do_assign` | · nested | The `$PK` interpreter: translate NONMEM operators (`**`→`^`, `.EQ.`, `.GE.`, `.AND.`, `.NOT.`, …) to R, evaluate a fragment in the THETA/ETA/covariate environment, test whether the innermost `IF`/`ELSEIF`/`ELSE` branch is active, and assign `LHS = RHS` into that environment. This is what makes categorical `IF(COV.EQ.x)` blocks work with no level→theta table | expression text / one `$PK` line | translated string / evaluated value / `logical` / `invisible()` | writes into the eval env `e`; **`skipped <<-`** accumulates un-evaluable lines for the closing `warning()` | nested in `apply_covariate_model` |

**Notes:** No longer a reconciliation target — since the read path was rewritten (2026-07-20) it evaluates the model's own `$PK` instead of re-deriving the covariate algebra, so the hardcoded factor list (`power`/`linear`/`exponential`, categorical `1 + beta`) and the registry `r_eval` fields are **gone**. It still parses `$THETA` order/names and the `$PK` `IF(COV.EQ.x)` blocks (level→theta relies on that numeric-level parse — the `$THETA` name carries the decoded label, not the level). Contract: the downstream mrgsolve model must use the returned THETA columns as **natural-scale structural params** (no `EXP()`, no covariate terms) or the effect is double-counted. `man/apply_covariate_model.Rd` exists.

### R/build-scenario-parameters.R
| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `build_scenario_parameters(model, covariate_search, thetas, data, percentiles=c(0.05,0.95), models_folder="models", lookup=NULL, spec=NULL, id_col="ID", wrap_width=30, scenario_table_path=paste0("scenario_table_",model,".rds"))` | ✓ | For each scenario, apply covariate factors to sampled THETAs; persist table | thetas, search table, data | named list of `data.frame`s (one per scenario; elem1 = typical subject) | **`saveRDS`** to `scenario_table_<model>.rds` (cwd); `dir.create`; `stop` | calls `create_covariate_table`, `apply_covariate_model` |

**Notes:** Pure orchestration. Exported and in NAMESPACE. `scenario_table_path` accepts a directory as well as a file path. Attaches `scenario_table` / `covariate_lookup` attributes to the returned list, which `plot_parameter_forests` reads back.

### R/forest-plots.R — simulate scenario profiles
| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `simulate_scenario_profiles(param_sets, mod, dose, start=0, end=24, delta=0.1, verbose=TRUE)` | ✓ | Simulate every sample of every scenario under a common dose; stack + tag by scenario | param_sets, mrgsolve `mod`, `ev` dose | `data.frame` (Scenario ordered factor, ID, time, captured cols) | `mrgsolve::mrgsim(output="df")`; `purrr::imap_dfr`; `stop` | upstream `build_scenario_parameters`; downstream `plot_exposure_forest` |

**Notes:** `mrgsolve` is in `Suggests:`. `inherits(dose,"ev")` couples to mrgsolve. Exported and in NAMESPACE. Caller builds `mod`/`dose`. **In-package `mrgsim()` must use `output="df"`** — `as.data.frame(mrgsims)` fails inside a package namespace (base S3 can't reach mrgsolve's S4 method).

### R/forest-plots.R — exposure forest plot
| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `plot_exposure_forest(data, metric=c("AUC","Cmax","Cmin"), ss=TRUE, ClinicalRelevanceLow=0.8, ClinicalRelevanceHigh=1.25, outer_range=c(0.5,2), reference="Typical subject", scenario=NULL, fontsize=9, title=NULL, x_lim=NULL, x_breaks=NULL, typical_subject=TRUE, filename=NULL, output_format=c("emf","png"), metric_info=NULL, model=NULL, percentiles=c(0.05,0.95), lookup=NULL, spec=NULL, width, height)` | ✓ | Forest plot of one metric normalised to the reference-scenario median (box 2.5/25/50/75/97.5). `scenario` (replaced `scenario_order`) sets axis order **and** the typical-subject subtitle — must be a **data.frame** (or path) for either to apply, and for any table to be written; `metric_info` triggers the absolute+relative summary tables. **`ss=TRUE` appends "ss" to the metric in the auto title AND to the saved file stem** (`AUC_forest` → `AUC_forest_ss.emf`; skipped if the stem already ends in "ss"), so steady-state and single-dose plots don't overwrite each other. **Ratio-axis control is split in two:** `x_lim` sets the limits (`coord_flip(ylim=)`), `x_breaks` sets the tick positions (`scale_y_continuous(breaks=)`, added as `NULL` — a no-op — when not given, so ggplot2's default breaks stand); breaks outside `x_lim` are simply not drawn | data, metric, bands, filename | `ggplot` (invisibly; saved if `filename`) | **file write** `ggsave` — **`devEMF::emf`**(undeclared)/png; `tools::file_path_sans_ext`; `stats::`; `dplyr::` | calls `theme_forest`, `boxquantile`, `.load_if_path`, `.spec_to_lookup`, `.resolve_quantity_info`, `.require_quantity_labels`, `.scenario_summary_table`; upstream `simulate_scenario_profiles`, `plot_parameter_forests` |
| `theme_forest(base_size=12, base_family="")` | ✓ | `theme_bw()` variant | sizes | ggplot2 theme | `grid::unit` | called-by `plot_exposure_forest` |
| `boxquantile(y)` | · | Box stats at 2.5/25/50/75/97.5 | y | named numeric length-5 | `stats::quantile` | `stat_summary` |

**Notes:** `grid`/`tools`/`grDevices` un-declared base-priority (`devEMF` is in `Suggests:`). `plot_exposure_forest` + `theme_forest` are exported and **in NAMESPACE**. Hardcoded palette + band strings. Gained `x_lim` (fix the ratio axis), `x_breaks` (tick positions on that same axis), `scenario` (merged axis order + typical-subject subtitle), `outer_range` (optional 0.5–2 band) and the summary-table arguments.

### R/forest-plots.R — parameter forest plots
_Second forest branch: covariate effects on the structural parameters themselves, straight from `build_scenario_parameters` output — no mrgsolve simulation involved._

| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `plot_parameter_forests(param_sets, model, parameters=NULL, output_plot_folder="results/figure", output_rds_folder=NULL, output_format=c("emf","png"), combined_pdf=TRUE, width=6, height=6, outer_range=NULL, x_breaks=NULL, typical_subject=TRUE, param_info=NULL, scenario=NULL, reference="Typical subject", percentiles=c(0.05,0.95), lookup=NULL, spec=NULL, models_folder="models", verbose=TRUE, ...)` | ✓ | One forest per structural parameter, each showing **only the scenarios that move it** (reference + scenarios whose median differs). **Always skips parameters with no covariate effect** (flat across scenarios — e.g. a non-beta theta like `add_error`); not optional. Plot and RDS outputs go to separate folders. Only `outer_range`, `x_breaks`, `typical_subject`, `output_format`, `width`/`height` are forwarded **by name** to `plot_exposure_forest` (so every parameter forest shares the same tick positions); anything else (`x_lim`, `fontsize`, the relevance bounds) rides on `...` | `param_sets` (object or `.rds` path), model | invisibly the plots; files written | **file write** plots (`emf`/`png`, optional combined PDF) + summary-table RDS; `dir.create` | calls `plot_exposure_forest` (once per parameter), `stack_scenario_parameters` (which reaches `.structural_param_names`), `.load_if_path`, `.spec_to_lookup`, `.resolve_quantity_info`, `.require_quantity_labels`, `.scenario_summary_table` |
| `stack_scenario_parameters(param_sets, model=NULL, models_folder="models")` | ✓ | Stack the named per-scenario list into one long frame with an ordered `Scenario` factor; with `model`, relabels `THETA1..n` to their `$PK` parameter names | `param_sets` (object or path), model | long `data.frame(ID, params…, Scenario)` | `stop()` | calls `.load_if_path`, `.structural_param_names` |
| `.structural_param_names(model_name, models_folder="models")` | · | Map structural THETA positions to `$PK` names (`c(THETA1="CL", THETA2="V2")`), mirroring `apply_covariate_model`'s THETA→param logic (leading theta on a line → LHS → strip `TV_`). Label-only: unresolvable positions are omitted and keep `THETAn` | model name, folder | named `character` | `readLines` | called-by `stack_scenario_parameters` (its only call site; `plot_parameter_forests` reaches it only through that) |

**Notes:** `build_scenario_parameters` attaches `attr(param_sets, "scenario_table")` + `attr(., "covariate_lookup")`, and `plot_parameter_forests` falls back to them — so **no `scenario`/`lookup` argument is needed** for the parameter table (only `param_info`); rebuild `param_sets` to get the attrs. `plot_exposure_forest` still needs an explicit `scenario=` (the summarise step drops attributes).

### R/covariate-boxplots.R
| Function | Exp | Purpose | Key inputs | Returns | Side effects | Calls / Called-by |
|---|---|---|---|---|---|---|
| `create_covariate_boxplots(data, spec, con=NULL, cat=NULL, type=c("AUC","Cmax","Cmin"), drug, param_info, stratification=NULL, total_panel=TRUE, output_folder="results/figure/simulations", width=NULL, height=NULL, base_size=10, verbose=TRUE, combined_pdf=TRUE, prefix=NULL, output_format=c("emf","png"), show_median=TRUE, show_n=TRUE, percent_change=FALSE, label_size=NULL, ss=TRUE)` | ✓ | Boxplots per metric×covariate (continuous→quartile bins `Qk:[lo,hi]`, categorical→decoded), optional right-hand `Total` panel pooling every subject into one "All subjects" box, per-cov `.emf`/`.png` + one combined multi-page PDF. **`ss=TRUE` (default) appends "ss" to the metric label on the y axis (`<drug> AUCss, <unit>`) and `_ss` to every saved stem** (`<drug>-<cov>-AUC_ss.emf`; PDF `<drug>-AUC-Cmax-Cmin_ss-<HHMM>.pdf`), skipped where the text already ends in "ss" — same contract as `plot_exposure_forest(ss=)`, so steady-state and single-dose runs don't overwrite each other | data (object or path), `spec` (yspec object or path), con/cat, type, `param_info` | nested list `results[[type]][[cov]]` = ggplot (invisible) | **file write** `.emf` via `devEMF`, `.png` via `ggsave`, `grDevices::cairo_pdf`; `dir.create`; `yspec::ys_load/ys_get_short_unit`; `grid::unit`; `cat()` progress when `verbose`; `stop` | calls `.load_if_path`, `.resolve_quantity_info`; otherwise standalone (not in the forest data flow) |

**Notes:** `devEMF` in `Suggests:`; `grid`/`grDevices` un-declared; `yspec` declared. Exported and in NAMESPACE. Hardcoded `results/figure/simulations`. `param_info` accepts a spec; the `Total` panel is optional and `type` is not locked to AUC/Cmax/Cmin. Defers categorical decode to `yspec::decode_dataset` (fails fast on raw numeric codes) — conceptually overlaps `create_covariate_table`'s decode but doesn't re-implement it.
