% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest-plots.R
\name{plot_parameter_forests}
\alias{plot_parameter_forests}
\title{Save a Covariate Parameter Forest for Every Structural Parameter}
\usage{
plot_parameter_forests(
  param_sets,
  model,
  parameters = NULL,
  output_folder = "results/figure",
  output_format = c("emf", "png"),
  combined_pdf = TRUE,
  width = 6,
  height = 6,
  models_folder = "models",
  verbose = TRUE,
  ...
)
}
\arguments{
\item{param_sets}{Named list from \code{\link[=build_scenario_parameters]{build_scenario_parameters()}}, given as
either the list itself OR a character path to an \code{.rds} file to load.}

\item{model}{Character. Model name - used to relabel THETA columns via its
\verb{$PK}, to name the output subfolder, and in the file names.}

\item{parameters}{Optional character vector selecting which parameters to
plot; \code{NULL} (default) plots every structural parameter column.}

\item{output_folder}{Base output directory; the model name is appended, so
files land in \code{output_folder/<model>/}. Default \code{"results/figure"}.}

\item{output_format}{Character vector, subset of \code{c("emf","png")}; which
per-parameter image format(s) to save. Default both. \code{.emf} via
\code{\link[devEMF:emf]{devEMF::emf()}}, \code{.png} via the \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} default device.}

\item{combined_pdf}{Logical; if \code{TRUE} (default) also write a single
multi-page PDF of every parameter forest to the same folder.}

\item{width, height}{Numeric. Figure size in inches. Default 6 x 6.}

\item{models_folder}{Character. Folder containing \code{model}. Default "models".}

\item{verbose}{Logical; print progress. Default \code{TRUE}.}

\item{...}{Further arguments passed to \code{\link[=plot_exposure_forest]{plot_exposure_forest()}} (e.g.
\code{reference}, \code{ClinicalRelevanceLow}, \code{ClinicalRelevanceHigh}, \code{fontsize}).}
}
\value{
Invisibly, a named list of the ggplot objects (one per parameter).
}
\description{
Convenience driver that builds and saves a covariate \strong{parameter forest} for
each structural parameter of a model, plus one combined PDF of them all. It
stacks the scenario parameter tables (\code{\link[=stack_scenario_parameters]{stack_scenario_parameters()}}, which
relabels the \code{THETA} columns to their \verb{$PK} names) and forests each parameter
with \code{\link[=plot_exposure_forest]{plot_exposure_forest()}} - no simulation or exposure metrics.

Files are written under \code{output_folder/<model>/} (created if missing): one
\code{<model>-forest-plot-<param>.emf}/\code{.png} per parameter, plus
\code{<model>-forest-plots.pdf} containing every parameter forest (one per page).
}
\examples{
\dontrun{
params <- build_scenario_parameters("run19", covariate_search, thetas, data)
plot_parameter_forests(params, model = "run19")
# -> results/figure/run19/run19-forest-plot-CL.emf/.png, ...,
#    results/figure/run19/run19-forest-plots.pdf
}
}
\seealso{
\code{\link[=stack_scenario_parameters]{stack_scenario_parameters()}}, \code{\link[=plot_exposure_forest]{plot_exposure_forest()}}
}
