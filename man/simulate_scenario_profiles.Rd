% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest-plots.R
\name{simulate_scenario_profiles}
\alias{simulate_scenario_profiles}
\title{Simulate Concentration-Time Profiles for Each Covariate Scenario}
\usage{
simulate_scenario_profiles(
  param_sets,
  mod,
  dose,
  start = 0,
  end = 24,
  delta = 0.1,
  verbose = TRUE
)
}
\arguments{
\item{param_sets}{Named list of data frames from \code{\link[=build_scenario_parameters]{build_scenario_parameters()}}
(each: \code{ID} + structural \code{THETA} columns; the list names label the scenarios
and are carried into a \code{Scenario} column of the output). Given as either the
list itself OR a character path to an \code{.rds} file to load.}

\item{mod}{A loaded mrgsolve model object (from \code{\link[mrgsolve:mread]{mrgsolve::mread()}}), compiled
by the caller before calling this function.}

\item{dose}{An mrgsolve event object (\code{\link[mrgsolve:ev]{mrgsolve::ev()}}) describing the dosing
regimen, applied to every subject, e.g.
\code{mrgsolve::ev(amt = 300, cmt = 1, ii = 12, addl = 5, ss = 1)}.}

\item{start, end, delta}{Numeric. Observation time grid for \code{\link[mrgsolve:mrgsim]{mrgsolve::mrgsim()}}.
Defaults \code{0}, \code{24}, \code{0.1}. For AUC over one steady-state interval set \code{end}
to one dosing interval (\code{ii}).}

\item{verbose}{Logical. When \code{TRUE} (default) print a \verb{[i/n] Simulating
scenario: <name>} progress line as each scenario is simulated. Set \code{FALSE}
to silence.}
}
\value{
A \code{data.frame} of stacked profiles - one row per
(scenario, sample, time) - with a \code{Scenario} column (an ordered factor, in
scenario definition order), \code{ID} (the sample), \code{time}, and the model's
captured columns (e.g. \code{CP}, \code{IPRED}, \code{DV}), ready for metric derivation,
e.g. \code{group_by(Scenario, ID) \%>\% summarise(AUC = ..., Cmax = ..., Cmin = ...)}.
}
\description{
Runs each scenario's parameter draws through an mrgsolve model under a
user-defined dosing regimen, and stacks the resulting concentration-time
profiles tagged by scenario. Consumes the named list from
\code{\link[=build_scenario_parameters]{build_scenario_parameters()}}: each element is one scenario's per-sample
parameters, and every sample is simulated as an individual subject (via
mrgsolve \code{idata}) under the common dose (via \code{events}).

The model must expose the parameters it needs as \verb{$PARAM} (e.g.
\code{THETA1..THETAn}); columns of the parameter tables matching those names
override them per subject. Covariate effects are already baked into the
parameters upstream by \code{\link[=apply_covariate_model]{apply_covariate_model()}}, so the mrgsolve model must
be structural only (no covariate terms).
}
\section{Memory}{

Holds every time point for every sample in every scenario
(~\code{n_scenarios * Nsamples * n_times} rows). Fine for modest \code{Nsamples}; at
\code{1e5}, simulate and reduce to metrics one scenario at a time.
}

\examples{
\dontrun{
mod  <- mrgsolve::mread("models/sim_model.cpp")   # loaded once, by the caller
dose <- mrgsolve::ev(amt = 300, cmt = 1, ii = 12, addl = 5, ss = 1)
profiles <- simulate_scenario_profiles(param_sets, mod, dose, end = 12)
}
}
\seealso{
\code{\link[=build_scenario_parameters]{build_scenario_parameters()}}
}
