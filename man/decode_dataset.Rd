% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{decode_dataset}
\alias{decode_dataset}
\title{Decode Categorical Columns Using a Variable Specification}
\usage{
decode_dataset(data, yaml_file, column_names)
}
\arguments{
\item{data}{A data frame containing the columns to decode.}

\item{yaml_file}{The variable specification: a loaded yspec object, or a raw
list from \code{\link[yaml:read_yaml]{yaml::read_yaml()}}. Each entry keyed by column name is expected
to carry \code{values} (the numeric codes) and \code{decode} (the matching labels).}

\item{column_names}{Character vector of column names in \code{data} to decode. A
name whose spec entry lacks \code{values}/\code{decode}, or that is not a column of
\code{data}, is left unchanged.}
}
\value{
\code{data}, with each requested column replaced by a \code{factor} of its
\code{decode} labels (levels in spec order). Any code not present in \code{values}
becomes \code{NA}.
}
\description{
Replaces the numeric codes of one or more categorical columns with their
decoded labels, using each column's \code{values} -> \code{decode} mapping from a
variable specification. Every decoded column becomes a \code{factor} whose levels
are the \code{decode} labels in spec order, so downstream plots and tables show
readable categories instead of raw codes.

The specification may be a loaded \pkg{yspec} object (from
\code{\link[yspec:ys_load]{yspec::ys_load()}}) \strong{or} a plain list read from a spec YAML with
\code{\link[yaml:read_yaml]{yaml::read_yaml()}}. In both cases the fields are read with \code{$}
(\code{spec[[col]]$values} / \code{$decode}): on a yspec column that resolves the yspec
accessor, and on a raw list it reads the list element - so the same call
works for either input. (Note: routing the same fields through \code{as.list()}
first does \strong{not} work on a yspec column - it bypasses the accessor and the
\code{values}/\code{decode} pairing shifts.)
}
\examples{
\dontrun{
spec  <- yspec::ys_load(here::here("data", "spec", "lookup.yml"))
flags <- yspec::pull_meta(spec, "flags")
dat   <- decode_dataset(dat, spec, c(flags$catcov))
}
}
