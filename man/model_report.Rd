% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-output-tables.R
\name{model_report}
\alias{model_report}
\title{Generate Parameter Table Report for Multiple Models}
\usage{
model_report(
  model_names,
  shrinkage = "etasd",
  models_folder = "models",
  spec_pk = NULL,
  lookup = NULL,
  bootstrap = FALSE,
  boot_results = NULL
)
}
\arguments{
\item{model_names}{Character vector of model names}

\item{shrinkage}{Type of shrinkage to report ("etasd", "etavr", "ebvsd", "ebvvr")}

\item{models_folder}{Path to models folder}

\item{spec_pk}{Optional yspec object for parameter formatting}

\item{lookup}{Optional covariate lookup spec used to decode categorical covariate
levels in the table labels. A \code{lookup.yaml}-shaped list (e.g.
\code{yaml::read_yaml("data/spec/lookup.yaml")}) where each covariate entry carries
\code{values} and \code{decode}. When \code{NULL} (default) or when a covariate/level has no
usable decode, the generic "level N" label is used.}

\item{bootstrap}{Logical. Report a single model beside a non-parametric
bootstrap. The final-model columns collapse to \code{Estimate} and
\verb{RSE (\%) [Shrinkage (\%)]}, an asymptotic \verb{95\% CI} column
(\verb{estimate ± 1.96 * SE}) is added, and the bootstrap \code{Median} and
\verb{95\% CI-bootstrap} follow under their own spanning header. Requires exactly
one model name and a \code{boot_results}. Default \code{FALSE} leaves the table as it
was.

The interval is built where the standard error lives and then mapped onto
the reported scale, so it carries the units of the Estimate beside it. Two
cases leave it empty: a parameter with no standard error (held at a FIX
value, or an unsuccessful covariance step), and an OMEGA whose lower bound
reaches a negative variance, which has no CV\% - roughly, once the printed
RSE passes the high twenties for a small OMEGA. The bound is left out
rather than floored at zero, which would state a limit the estimation did
not support.}

\item{boot_results}{Bootstrap summaries as a data frame, or a path to a
\code{.csv}/\code{.rds} holding one. Rows are matched to the report on NONMEM's own
parameter names (\code{THETA1}, \code{OMEGA(1,1)}, ...), so order does not matter and
extra rows are reported and skipped. Needs a parameter-name column
(\code{parameter_names}/\code{parameter}/\code{param}), a central-value column
(\code{median}/\code{estimate}/\code{value}) and one column whose name contains "CI",
holding the interval as \code{"[65.2-81.1]"} or \code{"[65.2, 81.1]"}. Values are used
as given: they must already be on the scale the report shows (CV\% for OMEGA
diagonals, back-transformed THETAs).}
}
\value{
flextable object with formatted parameter table
}
\description{
Generate Parameter Table Report for Multiple Models
}
