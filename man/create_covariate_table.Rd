% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariate-table.R
\name{create_covariate_table}
\alias{create_covariate_table}
\title{Create a Covariate Table (Null Patient + One-at-a-Time Variations)}
\usage{
create_covariate_table(
  model_name,
  covariate_search,
  data,
  percentiles = c(0.05, 0.95),
  models_folder = "models",
  id_col = "ID",
  lookup = NULL,
  wrap_width = 30
)
}
\arguments{
\item{model_name}{Character. Model name without extension, e.g. "run28".}

\item{covariate_search}{Data frame. Covariate-search table with columns
\code{COVARIATE}, \code{STATUS} ("con"/"cat"), \code{REFERENCE}, and \code{LEVELS}
(semicolon-separated levels for categoricals, e.g. "0;1;2"). If a
\code{cov_to_test} column is absent it is derived from \code{COVARIATE}/\code{PARAMETER}.}

\item{data}{Data frame. The analysis dataset used to compute continuous
covariate percentiles.}

\item{percentiles}{Numeric vector of probabilities in [0, 1] used to vary
continuous covariates. Default \code{c(0.05, 0.95)}.}

\item{models_folder}{Character. Folder containing the model. Default "models".}

\item{id_col}{Character. Subject ID column in \code{data}, used to de-duplicate to
one row per subject before computing percentiles. Default "ID".}

\item{lookup}{Optional named list (lookup.yaml-style) keyed by covariate, used
only to render the \code{Scenario} description. Each entry may hold \code{short} or
\code{label} (a human name for the covariate) and \code{values}/\code{decode} (to translate
a categorical level to its label). When absent, the covariate code and raw
level are used.}

\item{wrap_width}{Integer. If a \code{Scenario} string is longer than this many
characters, a line break is inserted so the "(...)" detail (continuous) or
the category level (categorical) moves onto a second line, which helps long
labels fit on plots. Set to \code{Inf} or \code{NULL} to disable. Default 30.}
}
\value{
A \code{data.frame} whose first column \code{Scenario} describes each row
("Typical subject" for the null patient, "Low/High \if{html}{\out{<covariate>}} (Nth
percentile)" for continuous, "\if{html}{\out{<covariate>}}: \if{html}{\out{<level>}}" for categorical),
followed by one column per covariate. Row 1 is the null patient (all
reference values); each further row is a single-covariate variation.
}
\description{
Builds a covariate table for covariate-effect / forest-plot style analyses.
The first row is the "null patient": every covariate held at its reference
value. Each subsequent row varies exactly one covariate while all others stay
at reference. Continuous covariates are varied across the requested data
percentiles; categorical covariates are varied across their non-reference
levels.

Covariates are taken from the model (its \verb{beta_<COV>_<PARAM>} THETA names).
Reference values, covariate type, and categorical levels come from the
covariate-search object. Percentiles for continuous covariates are computed
from the supplied dataset, using one row per subject.
}
\examples{
\dontrun{
tbl <- create_covariate_table(
  model_name       = "run28",
  covariate_search = search_state$covariate_search,
  data             = search_state$data_file,
  percentiles      = c(0.05, 0.95)
)
}
}
