% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build-scenario-parameters.R
\name{build_scenario_parameters}
\alias{build_scenario_parameters}
\title{Build Parameter Sets for the Typical Subject and Each Covariate Scenario}
\usage{
build_scenario_parameters(
  model,
  covariate_search,
  thetas,
  data,
  percentiles = c(0.05, 0.95),
  models_folder = "models",
  lookup = NULL,
  spec = NULL,
  id_col = "ID",
  wrap_width = 30,
  scenario_table_path = paste0("scenario_table_", model, ".rds")
)
}
\arguments{
\item{model}{Character. Model name without extension, e.g. "run28".}

\item{covariate_search}{Covariate-search table (consumed by
\code{\link[=create_covariate_table]{create_covariate_table()}} and \code{\link[=apply_covariate_model]{apply_covariate_model()}}), given as either a
\code{data.frame} OR a character path to a \code{.csv}/\code{.rds} file to load.}

\item{thetas}{Sampled THETA vectors from \code{\link[=sample_individual_thetas]{sample_individual_thetas()}} (columns
\code{THETA1}..\code{THETAn}, absolute scale; covariate-beta columns must be present,
as they drive the factors), given as either a \code{data.frame} OR a character
path to a \code{.csv}/\code{.rds} file to load.}

\item{data}{Analysis dataset (used by \code{\link[=create_covariate_table]{create_covariate_table()}} to compute
continuous-covariate percentiles), given as either a \code{data.frame} OR a
character path to a \code{.csv}/\code{.rds} file to load. It is used \strong{as-is}: the
model's \verb{$DATA} \code{IGNORE}/\code{ACCEPT} filter is not applied, so pre-filter it to
the modelling population before calling (the function warns to this effect).
No decoding is needed - continuous covariates use numeric percentiles and
categorical scenarios come from \code{covariate_search$LEVELS} + \code{lookup}.}

\item{percentiles}{Numeric vector of probabilities in [0, 1] for continuous
covariates, passed straight to \code{\link[=create_covariate_table]{create_covariate_table()}}. User-selectable.
Default \code{c(0.05, 0.95)}.}

\item{models_folder}{Character. Folder containing the model. Default "models".}

\item{lookup}{Optional \code{lookup.yaml}-style named list keyed by covariate
(the same object you read with \code{read_yaml()}), forwarded to
\code{\link[=create_covariate_table]{create_covariate_table()}} so scenario labels show covariate \strong{units} and
\strong{decoded categorical levels}. When \code{NULL} (default), raw covariate codes
and levels are shown.}

\item{spec}{Optional yspec object, or a path to a spec YAML, forwarded to
\code{\link[=create_covariate_table]{create_covariate_table()}} - a convenient alternative to \code{lookup}: the
short/unit/values/decode are taken from the spec so scenario labels show
units and decoded levels. Any explicit \code{lookup} entries override the spec.}

\item{id_col}{Character. Subject-ID column used to reduce \code{data} to one row
per subject before computing percentiles. Forwarded to
\code{\link[=create_covariate_table]{create_covariate_table()}}. Default "ID".}

\item{wrap_width}{Integer. Scenario-label wrap width, forwarded to
\code{\link[=create_covariate_table]{create_covariate_table()}}. Default 30; \code{Inf} or \code{NULL} disables wrapping.}

\item{scenario_table_path}{Character or NULL. Where to save the scenario table
(the \code{\link[=create_covariate_table]{create_covariate_table()}} result, in definition order) as an RDS for
later reference - e.g. to restore the scenario order in
\code{\link[=plot_exposure_forest]{plot_exposure_forest()}} via \code{scenario_order}. May be a full file path
(".../foo.rds") OR a \strong{directory} (a trailing slash, an existing folder, or
any path without an \code{.rds} extension) - for a directory the default file
name \code{scenario_table_<model>.rds} is written inside it. Defaults to
\code{paste0("scenario_table_", model, ".rds")}; missing parent folders are
created (nested). Pass \code{NULL} to skip saving.}
}
\value{
A named list of data frames, one per scenario (named by the scenario
description). Each holds \code{ID} (sample index) plus the structural THETA
columns with that scenario's covariate factors applied. The first element is
the typical subject.
}
\description{
Takes an already-sampled set of THETA vectors (from
\code{\link[=sample_individual_thetas]{sample_individual_thetas()}}) and, for each covariate scenario, \strong{updates the
structural parameters} by applying the model's covariate relationships with
\code{\link[=apply_covariate_model]{apply_covariate_model()}}. Scenarios come from \code{\link[=create_covariate_table]{create_covariate_table()}}:
row 1 is the typical subject (all covariates at reference), and each further
row varies one covariate across the requested percentiles (continuous) or
levels (categorical).

The sampling of parameter uncertainty is already done upstream - this step
only re-expresses those same draws under each covariate condition. Because the
typical subject sits at reference covariate values, every covariate factor is
1 there, so its parameter set is just the sampled structural thetas unchanged.
Each sample keeps the same \code{ID} across all scenarios, so a later step can pair
a scenario with the typical subject sample-by-sample.

The scenario table (from \code{\link[=create_covariate_table]{create_covariate_table()}}) is also saved to
\code{scenario_table_path} as an RDS, so a later plot can restore the scenario
order from it.
}
\examples{
\dontrun{
thetas <- sample_individual_thetas("run28", Nsamples = 1000)
param_sets <- build_scenario_parameters(
  model            = "run28",
  covariate_search = search_state$covariate_search,
  thetas           = thetas,
  data             = search_state$data_file,
  percentiles      = c(0.05, 0.95)
)
names(param_sets)       # "Typical subject", "High WT (95th ...)", ...
head(param_sets[[1]])   # typical-subject parameter draws
}
}
\seealso{
\code{\link[=sample_individual_thetas]{sample_individual_thetas()}}, \code{\link[=create_covariate_table]{create_covariate_table()}},
\code{\link[=apply_covariate_model]{apply_covariate_model()}}
}
