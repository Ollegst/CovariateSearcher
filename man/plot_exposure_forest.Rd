% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest-plots.R
\name{plot_exposure_forest}
\alias{plot_exposure_forest}
\title{Forest Plot of a Covariate Effect on an Exposure Metric}
\usage{
plot_exposure_forest(
  data,
  metric = c("AUC", "Cmax", "Cmin"),
  ss = TRUE,
  ClinicalRelevanceLow = 0.8,
  ClinicalRelevanceHigh = 1.25,
  outer_range = c(0.5, 2),
  reference = "Typical subject",
  scenario = NULL,
  fontsize = 9,
  title = NULL,
  typical_subject = TRUE,
  filename = NULL,
  output_format = c("emf", "png"),
  width,
  height
)
}
\arguments{
\item{data}{A \code{Scenario} column and the metric column (\code{AUC}, \code{Cmax}, or
\code{Cmin}), one row per sample per scenario. Given as either a \code{data.frame} OR
a character path to a \code{.csv}/\code{.rds} file to load.}

\item{metric}{Character. The column of \code{data} to forest (its distribution is
normalised to the reference scenario's median). Defaults to the exposure
metrics \code{"AUC"}/\code{"Cmax"}/\code{"Cmin"}, but any numeric column works - e.g. a
structural parameter column (\code{"CL"}, \code{"V2"}, ...) from
\code{\link[=stack_scenario_parameters]{stack_scenario_parameters()}} for a parameter forest.}

\item{ss}{Logical. Steady state? Adds an "ss" suffix to the metric in the
title (e.g. "AUC" vs "AUCss"). Default \code{TRUE}.}

\item{ClinicalRelevanceLow, ClinicalRelevanceHigh}{Numeric. Bounds of the
shaded (inner) clinical-relevance band (ratio scale). Defaults \code{0.8},
\code{1.25}.}

\item{outer_range}{Numeric length-2 vector, or \code{NULL}. Bounds of a second,
wider shaded reference band (ratio scale) drawn behind the inner one.
Default \code{c(0.5, 2)}; \code{NULL} draws only the inner clinical-relevance band.}

\item{reference}{Character. Scenario used as the reference (denominator of the
ratio and highlighted colour). Default "Typical subject".}

\item{scenario}{Optional. The scenario table from \code{\link[=build_scenario_parameters]{build_scenario_parameters()}}
(a \code{data.frame}, or a path to a saved \code{.rds}/\code{.csv}): its \code{Scenario} column
sets the axis order and its \code{"typical_subject"} attribute supplies the
subtitle - handy for exposure forests, whose metrics table (built with
\code{group_by()}/\code{summarise()}) has lost both. May also be a plain character
vector giving just the axis order. When \code{NULL} (default) the factor levels
of \code{data$Scenario} are used if it is a factor (as
\code{\link[=simulate_scenario_profiles]{simulate_scenario_profiles()}} sets them), else first-appearance order.}

\item{fontsize}{Numeric. Base font size. Default 9.}

\item{title}{Character or NULL. Plot title; when NULL it is built from
\code{metric} and \code{ss} ("Covariate effects on \if{html}{\out{<metric>}}\link{ss}").}

\item{typical_subject}{Controls the reference-subject subtitle under the
title. \code{TRUE} (default) uses the ready-made string that
\code{\link[=create_covariate_table]{create_covariate_table()}} / \code{\link[=build_scenario_parameters]{build_scenario_parameters()}} attach as
\code{attr(data, "typical_subject")} (nothing shown if that attribute is
absent); \code{FALSE}/\code{NULL} shows no subtitle; a character string is shown
verbatim (custom text, e.g. "Typical subject: 70 kg male, ECOG 0").}

\item{filename}{Character or NULL. Base path for saved output; any extension
is stripped and one file per \code{output_format} is written as
\verb{<stem>.<format>}. Missing parent folders are created. \code{NULL} (default)
does not save.}

\item{output_format}{Character. Which format(s) to save when \code{filename} is
given: one or both of \code{"emf"} and \code{"png"}. Default both. \code{.emf} is written
with \code{devEMF::emf}, \code{.png} with the \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} default device.}

\item{width, height}{Numeric. Output size in inches. Mandatory (no default);
required whenever \code{filename} is given.}
}
\value{
The ggplot object (invisibly saved to \code{filename} when supplied).
}
\description{
Builds a forest plot of one exposure metric's covariate effects: each
scenario's metric distribution is normalised to the typical subject's median
and shown as a box (2.5 / 25 / 50 / 75 / 97.5 percentiles), with a reference
line at 1 and a shaded clinical-relevance band. Consumes an
already-calculated metrics table (\code{ID}, \code{AUC}, \code{Cmax}, \code{Cmin}, \code{Scenario}) -
e.g. a \code{group_by(Scenario, ID) \%>\% summarise(...)} result, saved to RDS or in
memory.
}
\examples{
\dontrun{
raw <- readRDS("data/derived/exposure_metrics_run19.rds")
for (m in c("AUC", "Cmax", "Cmin")) {
  plot_exposure_forest(
    raw, metric = m, ss = TRUE,
    filename = paste0("results/run19/", m, "ss_forestplot"),
    width = 6, height = 6
  )
}
}
}
\seealso{
\code{\link[=theme_forest]{theme_forest()}}
}
