% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest-plots.R
\name{stack_scenario_parameters}
\alias{stack_scenario_parameters}
\title{Stack Scenario Parameter Tables into One Long, Scenario-Tagged Data Frame}
\usage{
stack_scenario_parameters(param_sets, model = NULL, models_folder = "models")
}
\arguments{
\item{param_sets}{Named list from \code{\link[=build_scenario_parameters]{build_scenario_parameters()}} (each element:
\code{ID} + structural \code{THETA} columns), given as either the list itself OR a
character path to an \code{.rds} file to load.}

\item{model}{Optional model name (as for \code{\link[=build_scenario_parameters]{build_scenario_parameters()}}); when
supplied, THETA columns are relabelled to their \verb{$PK} parameter names.}

\item{models_folder}{Character. Folder containing \code{model}. Default "models".}
}
\value{
A long \code{data.frame}: \code{ID}, the parameter columns (named \code{THETA1..} or
the mapped parameter names), and an ordered-factor \code{Scenario} column.
}
\description{
Turns the named list from \code{\link[=build_scenario_parameters]{build_scenario_parameters()}} into a single long
data frame with a \code{Scenario} column (an ordered factor in scenario-definition
order), ready to feed straight to \code{\link[=plot_exposure_forest]{plot_exposure_forest()}} for a \strong{parameter
forest} - the covariate effect on a structural parameter (CL, V, ...), with
no simulation or exposure metrics. It is the pre-simulation counterpart of the
stacking \code{\link[=simulate_scenario_profiles]{simulate_scenario_profiles()}} does internally.

When \code{model} is supplied, the \code{THETA1..THETAn} columns are renamed to the
model's parameter names (e.g. \code{THETA1 -> CL}) by reading its \verb{$PK}, so the
forest axis/title read the real names. Positions that cannot be resolved keep
their \code{THETAn} name.
}
\examples{
\dontrun{
params <- build_scenario_parameters("run19", covariate_search, thetas, data)
pf <- stack_scenario_parameters(params, model = "run19")
plot_exposure_forest(pf, metric = "CL", ss = FALSE, width = 6, height = 6)
}
}
\seealso{
\code{\link[=build_scenario_parameters]{build_scenario_parameters()}}, \code{\link[=plot_exposure_forest]{plot_exposure_forest()}}
}
