% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialization.R
\name{validate_param_transformations}
\alias{validate_param_transformations}
\title{Validate parameter transformations for population covariates}
\usage{
validate_param_transformations(
  covariate_search,
  data_file,
  id_col,
  model_name,
  models_folder = "models"
)
}
\arguments{
\item{covariate_search}{data.frame with COVARIATE, PARAMETER, TIME_DEPENDENT.}

\item{data_file}{data.frame with the analysis dataset (for the time-varying check).}

\item{id_col}{Character. Subject identifier column name.}

\item{model_name}{Character. Base model name (without extension).}

\item{models_folder}{Character. Folder containing the model (default "models").}
}
\value{
Invisibly \code{TRUE} if all population parameters are classifiable.
}
\description{
For every parameter that will receive a POPULATION (time-constant) covariate,
checks that the base model writes it in a recognised form -- normal-scale
\code{PARAM = TV * EXP(ETA)} or log-scale \code{PARAM = EXP(TV + ETA)} -- so
\code{model_add_cov} knows whether to render the covariate multiplicatively
(\code{*}) or additively (\code{+}). Fails fast at initialization on any
parameter whose parameterization cannot be classified. Time-varying covariates
are placed on the individual parameter line (always multiplicative) and so do
not require classification.
}
