% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scm-forward.R
\name{run_scm_covariate_search_forward}
\alias{run_scm_covariate_search_forward}
\title{Execute complete forward selection covariate modeling workflow}
\usage{
run_scm_covariate_search_forward(
  search_state,
  base_model_id = "run1",
  max_steps = NULL,
  ofv_threshold = NULL,
  rse_threshold = NULL,
  max_wait_minutes = 120,
  auto_submit = TRUE,
  auto_retry = TRUE
)
}
\arguments{
\item{search_state}{List containing covariate search state and configuration}

\item{base_model_id}{Character. Starting base model (default: "run1")}

\item{max_steps}{Integer. Maximum steps (NULL = unlimited, bounded by covariates)}

\item{ofv_threshold}{Numeric. OFV improvement threshold (uses config if NULL)}

\item{rse_threshold}{Numeric. Maximum RSE threshold (uses config if NULL)}

\item{max_wait_minutes}{Numeric. Maximum wait time per step (default: 120)}

\item{auto_submit}{Logical. Whether to automatically submit models (default: TRUE)}

\item{auto_retry}{Logical. Whether to enable automatic retry (default: TRUE)}
}
\value{
List with updated search_state and forward selection results
}
\description{
Runs dynamic forward selection starting from base model,
continuing until no significant improvements found or no covariates remain.
Updates search database and configuration with results.
}
\details{
Run SCM Forward Selection
}
